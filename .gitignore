node_modules/
dist/
dist-exe/
.build-cache/
*.tsbuildinfo
build/
target/
src-tauri/target/
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm
.env
.env.*
*.log
.DS_Store
Thumbs.db
/SRC/
/.tmp-appdata/

# Tauri updater signing key — MUST NEVER be committed. The .pub file is safe
# (it's embedded in tauri.conf.json), only the private .pem matters here.
.tauri-updater-key.pem
