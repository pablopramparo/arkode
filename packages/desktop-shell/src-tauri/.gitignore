# Generated by Cargo
# will have compiled files and executables
/target/
/gen/schemas

# Built via `pnpm --filter engine-cli build:exe`, copied in before `tauri build` — see CLAUDE.md's "Packaging" section.
/binaries/

# Vendored pg_dump/pg_restore/psql, fetched by prepare-pg-tools.mjs before `tauri build`.
/resources/
