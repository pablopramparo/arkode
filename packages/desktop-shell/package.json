{
  "name": "desktop-shell",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "prepare-sidecar": "node scripts/prepare-sidecar.mjs",
    "prepare-pg-tools": "node scripts/prepare-pg-tools.mjs",
    "prepare-restic": "node scripts/prepare-restic.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "extract-zip": "^2.0.1"
  }
}
