# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Current state

[project.md](project.md) is a Spanish-language product/technical spec for a Windows desktop app originally called, per its own opening line, "provisionalmente **Codebius Backup Manager**". Treat it as the source of truth for requirements; this file summarizes and updates it so you don't have to re-read it in full each session, but re-check `project.md` directly for exact wording/edge cases before making product decisions.

**Real product name, confirmed 2026-08-24: "arkode" (by Codebius), styled lowercase throughout** — not the placeholder from `project.md`'s first line. Confirmed by the user providing actual designed logo assets (`packages/ui/src/assets/arkode-logo-completo.png` — full lockup, used in the sidebar; `arkode-isotipo.png`/`public/favicon.png` — icon mark, used as the favicon), sourced from `/SRC/` at the repo root (gitignored — that's raw design-source, not a build input). The UI's window title and sidebar branding say "arkode" / "arkode by codebius" (with a footer link to codebius.com). **The rename is now complete everywhere, done 2026-08-24**: `engine-cli`'s own description string and the Task Scheduler folder name (`windowsTaskScheduler.ts`'s `scheduledTaskNameForBackupTask`) now say "arkode," and the app-data folder moved from `%PROGRAMDATA%\CodebiusBackupManager\` to `%PROGRAMDATA%\arkode\` (`paths.ts`'s `APP_DIR_NAME`) — including migrating this dev machine's real folder (it held only an effectively-empty SQLite DB from prior verification passes, so there was nothing of substance at risk; still moved deliberately rather than just letting the app recreate an empty one, since the general principle — never silently lose what's already on disk — applies regardless of how much was actually there). **Rebuilt and reinstalled, 2026-08-25**: the real `perMachine` install on this dev machine was bumped to v0.1.3, rebuilt from the renamed source, and updated via the real self-update flow (`Buscar actualizaciones` → `Descargar e instalar` → `Reiniciar ahora`), confirmed both by the UI showing "Versión actual: 0.1.3" and by `HKLM:\...\Uninstall\arkode`'s `DisplayVersion`. It now genuinely reads/writes `%PROGRAMDATA%\arkode\`, matching the source. Confirmed clean: no Windows Scheduled Task exists under either `\CodebiusBackupManager\` or `\arkode\` (a full non-elevated `schtasks /query` folder listing showed neither — the one known live registration from the day of the rename had already been uninstalled right after verifying it).
  - **Two build-environment issues hit and fixed while doing this, both caused by the folder rename itself (worth knowing if this ever needs redoing)**: (1) `pnpm install` was broken — `node_modules` held stale absolute paths from the old folder name — fixed with a full reinstall (`CI=true pnpm install`, needed non-interactively to consent to purging `node_modules`), which also transparently re-triggered `better-sqlite3`'s native rebuild. (2) `tauri build` then failed separately, deeper in: Cargo's `target/release/build/.../out/` cache also had stale absolute paths baked in from a build script (`failed to read plugin permissions: ... E:\DEV\backupmanager\...`) — fixed with `cargo clean` (in `packages/desktop-shell/src-tauri/`) followed by a full rebuild (~4 min). Both are exactly the kind of cache invalidation a folder rename can silently cause in tools that embed absolute paths; a `git mv`-only rename wouldn't hit either — this was a real filesystem rename outside git.
  - **A real, not-yet-fixed gap found while reinstalling**: the desktop-shell's sidecar-kill logic (`RunEvent::ExitRequested` killing the spawned `engine-cli.exe` child, described below) isn't airtight — a leftover/orphaned `engine-cli.exe` process (from this same investigation's own earlier manual `app.exe` launch/close cycle) held its own exe file locked, which made the NSIS installer fail outright on `Extrayendo: resources\pgsql\bin\zlib1.dll`'s neighbor file with an "Error abriendo archivo para escribir" retry dialog until the orphaned process was killed by hand (`Stop-Process`). This is specifically dangerous for the **silent** auto-update path (`downloadAndInstall()` runs the installer with `/S`, no visible retry dialog) — if a sidecar is ever left orphaned when that runs, the update could fail silently or leave a partially-overwritten install, with no dialog for the user to react to. Root cause not fully diagnosed (whatever closed `app.exe` that time didn't route through the graceful `WindowEvent`→`RunEvent::ExitRequested` chain that normally kills the child too — plausible causes include a forceful process kill bypassing that event, though it wasn't pinned down further since this session's actual close path was an atypical manual `Start-Process` launch, not the normal user flow). **Fixed the same day, confirmed with the user first.** `installer-hooks.nsh` gained `NSIS_HOOK_PREINSTALL` (and, for the same reason, `NSIS_HOOK_PREUNINSTALL`) running `taskkill /F /IM engine-cli.exe` then `taskkill /F /IM app.exe` before any file is touched — a backstop that removes the dependency on the graceful `RunEvent::ExitRequested` shutdown path entirely, safe to run even when neither process exists (a failed taskkill is simply ignored, not fatal to the script). **Verified for real against the exact failure that was hit, not just "should work"**: bumped to v0.1.4, force-killed the running `app.exe` (`Stop-Process -Force`, simulating whatever non-graceful death orphaned the sidecar originally) while leaving `engine-cli.exe` running and confirmed by hand it was genuinely lock-holding (`[System.IO.File]::Open(..., 'Write')` threw access-denied), then ran the new installer **silently** (`/S` — the same mode the real auto-updater uses, deliberately not the interactive mode that just shows a recoverable dialog) and confirmed: exit code 0, no orphaned processes left afterward, and `HKLM:\...\Uninstall\arkode`'s `DisplayVersion` read `0.1.4` — proving the install actually completed rather than silently no-op'ing past a locked file. Published as a real GitHub Release (`v0.1.4`) the same way as `v0.1.3`.

A pnpm workspace now exists with three implemented packages — `packages/engine-core` (the backup engine, pure TypeScript, zero UI dependency), `packages/engine-cli` (a commander CLI wrapping it), and `packages/ui` (a React web app, all 8 v1 screens — see "UI" below), and `packages/desktop-shell` (a Tauri wrapper around `packages/ui`, dev mode verified working end-to-end as a real native window — see "Desktop shell (Tauri)" below; still no production build/installer or OS-integration plugins). **All three backup strategies are implemented** and verified end-to-end against real local targets (Windows OpenSSH Server for the first two; real local PostgreSQL 18 and MySQL/WAMP 9.1 instances for the third, with a full dump→restore→data-matches round trip): **`fetch_existing`** (SFTP: detect an existing remote dump → download → validate → record), **`remote_dump`** (SSH: run a configured remote command to generate the dump → download → optionally delete the remote file → validate → record), and **`direct_dump`** (connect straight to Postgres/MySQL from this PC with SecretStore-held credentials, run `pg_dump`/`mysqldump` locally → validate → record). Verified behaviors include host-key confirmation, incremental checksumming, `.part`-then-rename handling, no redundant re-downloads (fetch_existing), correct nonzero-exit-code/auth-failure handling (all three), interrupted-run recovery, and the app-level concurrency lock. Retention enforcement is also implemented (see "Retention" below), `engine-core` has an automated vitest suite (see "Automated tests" below) covering the orchestrator and retention logic with fakes/real-temp-filesystem, every connection type (SFTP/SSH/Postgres/MySQL) can be tested without producing a download or dump (see "Connection testing" below), client configuration can be exported/imported as portable JSON with secrets deliberately excluded (see "Configuration export/import" below), and each task can be given a daily schedule and registered as a real Windows Scheduled Task that survives the GUI being closed (see "Scheduler" below). The manual end-to-end runs against real infrastructure remain the standard for the transport/dump-client adapters themselves. All 7 top-level v1 UI screens (Dashboard, Clientes, Conexiones, Tareas, Historial, Logs, Configuración) plus the per-client Detalle de cliente drill-down (reached from Clientes) now exist — see "UI" below. The Tauri desktop shell has its first working milestone (see "Desktop shell (Tauri)" below); its OS-integration plugins and a real production build/installer remain from the "Not yet built" list.

## Commands

```
pnpm install                      # from repo root
pnpm --filter engine-core build   # tsc -b + copies migrations/*.sql into dist/
pnpm --filter engine-cli build    # tsc -b (depends on engine-core's build output)
pnpm -r build                     # build both

pnpm --filter engine-core test    # vitest run — the only package with tests today
pnpm test                         # from repo root: pnpm -r test

# Run the CLI (from repo root, after building both packages):
node packages/engine-cli/dist/index.js <command>
#   migrate | client:create | transport:create-sftp | transport:create-ssh
#   | database-connection:create | task:create [--strategy fetch_existing|remote_dump|direct_dump]
#   [--retention-count <n>] [--retention-days <n>]
#   | task:run <taskId> | task:test-connection <taskId>
#   | transport:test <transportId> | database-connection:test <databaseConnectionId>
#   | client:test-connections <clientId>
#   | config:export [--client <id> (repeatable) | --all] [--output <file>]
#   | config:import --file <path>
#   | task:set-schedule <taskId> [--time HH:MM] [--disable]
#   | run-due [--task <taskId>]
#   | scheduler:install <taskId> --password <winPassword> [--username <domainUser>]
#   | scheduler:uninstall <taskId> | scheduler:status <taskId>
#   | retention:history <taskId> | status [--json]
#   | pg-tools:register --pg-version <v> --pg-dump-path <path> --pg-restore-path <path>
#   | pg-tools:list | pg-tools:unregister --pg-version <v>
#   | mysql-tools:register --mysql-version <v> --mysqldump-path <path> | mysql-tools:list | mysql-tools:unregister --mysql-version <v>
#   | mariadb-tools:register --mariadb-version <v> --mariadb-dump-path <path> | mariadb-tools:list | mariadb-tools:unregister --mariadb-version <v>
#   | task:test-compatibility <taskId>   # direct_dump only — connection + version + tool-availability gate
#   | task:set-schedule <taskId> --time HH:MM [--force]   # --force skips the direct_dump compatibility gate
#   | serve [--port <port>]   # dev-only local HTTP server for the UI, see "UI" below
```

```
# UI (packages/ui), from repo root:
pnpm --filter engine-cli exec node dist/index.js serve --port 4287   # start the status server first (needs engine-cli built)
pnpm --filter ui dev                                                  # Vite dev server, http://localhost:5173
pnpm --filter ui build                                                # production build (not wired into any packaging yet)
```

`direct_dump` tasks also need `PG_DUMP_PATH` (for `dbEngine: postgres`), `MYSQLDUMP_PATH` (for `dbEngine: mysql`), and/or `MARIADB_DUMP_PATH` (for `dbEngine: mariadb`, pointing at `mariadb-dump.exe` specifically — not `mysqldump.exe`, see the "direct_dump tool version management" note for why they're not interchangeable) env vars pointing at the respective local binaries — same dev-time pattern as `PG_RESTORE_PATH` for validation, no installer-vendoring yet. Connection testing (`transport:test`/`database-connection:test`/`client:test-connections`/`task:test-connection` on a direct_dump task) needs `PSQL_PATH`/`MYSQL_CLI_PATH` similarly — these are the interactive/batch client binaries (`psql.exe`/`mysql.exe`), bundled alongside `pg_dump`/`mysqldump` but distinct from them; a trivial `SELECT 1` verifies connectivity + auth without producing a dump.

`CODEBIUS_APP_DATA_DIR` overrides where the engine reads/writes its SQLite DB and logs (normally `%PROGRAMDATA%\arkode\` — moved from `%APPDATA%` on 2026-08-24, see "Secrets storage"; folder renamed from `CodebiusBackupManager` to `arkode` later the same day, see "Current state") — set it to a throwaway directory for manual CLI testing so you never touch the real app-data location. The env var's own name is unchanged (it's an internal override name, not user-facing branding).

## Automated tests

`packages/engine-core/test/` (vitest, run via `pnpm --filter engine-core test`). Real SQLite (in-memory, via `test/helpers/testContext.ts`, which mirrors `engine-cli`'s `buildContext()`) and real temp directories (`test/helpers/tempDir.ts`) throughout — no mocking of the DB or filesystem. What's covered:

- **`retention/applyRetention.spec.ts`** — the highest-value suite: count-based, days-based, and combined-policy pruning; the two hard invariants (never delete without a later real backup, never go to zero); and dedicated regression tests for the two real bugs found while building retention (duplicate deletion records on repeated passes; no-op Success runs defeating the survivor floor — see the "Retention" section above).
- **`orchestrator/runBackupTask.spec.ts`** — the core pipeline (success, checksum fallback, `NoNewDumpAvailableError` handling, validation/empty-file failure, the app-level concurrency lock, stale-run recovery, orphaned `.part` cleanup, retention wiring) using a fake `BackupStrategyExecutor` injected via `RunBackupTaskDeps.resolveExecutorOverride` — a seam that exists **only** for this purpose; production code never sets it. Real transports/SSH/SFTP/DB connections are deliberately not exercised here (that's what the manual end-to-end testing against real local targets, described throughout this file, is for) — this suite is about the orchestrator's own logic, strategy-agnostic.
- **`transports/hostKeyVerification.spec.ts`**, **`transports/outputPathTemplate.spec.ts`**, **`validators/genericValidator.spec.ts`**, **`validators/mysqlDumpValidator.spec.ts`**, **`db/repositories/tasksRepo.spec.ts`** — smaller focused suites for host-key trust logic, the date-token template resolver, minimum-bar file validation, mysqldump/mariadb-dump's header/completion-footer structural check, and the strategy/transport/database-connection type invariants.
- **`config/exportImportConfig.spec.ts`** — export/import round-tripping, the name-collision failure path, and a hand-built malformed export (a task referencing a transport name that doesn't exist) to confirm per-item errors don't abort the rest of the client's import.
- **`databaseConnections/postgresToolRegistry.spec.ts`** — `extractMajorVersion`'s pre-/post-PostgreSQL-10 versioning-scheme edge cases, and register/resolve/unregister round-tripping (including across separate registry instances backed by the same `settingsRepo`) for the pg_dump/pg_restore multi-version registry.
- **`databaseConnections/testDirectDumpCompatibility.spec.ts`** — every `toolCompatibility` branch (`registered`/`default-unverified`/`missing`) of the direct_dump pre-flight compatibility gate, via a `testConnectionOverride` injection seam (mirrors `RunBackupTaskDeps.resolveExecutorOverride`) so the connection+version step can be faked without a live DB server, with real temp files backing the "does the resolved tool path actually exist on disk" checks — including the `'registered'` branch for all three engines (postgres/mysql/mariadb).
- **`databaseConnections/mysqlToolRegistry.spec.ts`**, **`mariaDbToolRegistry.spec.ts`** — the mysqldump/mariadb-dump equivalents of `postgresToolRegistry.spec.ts`: their major.minor version-extraction edge cases (including MariaDB's `"-MariaDB"` suffix) and register/resolve/unregister round-tripping.
- **`scheduler/isTaskDue.spec.ts`**, **`scheduler/runDueTasks.spec.ts`**, **`scheduler/taskDefinitionXml.spec.ts`**, **`scheduler/redactPassword.spec.ts`** — the due-check's day/time/disabled logic, per-task failure isolation in `runDueTasks` (both a handled `Failed` run and an exception thrown before `runBackupTask` even creates a run row), the generated Task Scheduler XML's structure (both triggers, `LogonType=Password` not `S4U`, escaping), and the schtasks-error password-redaction helper.

Not covered by automated tests, and not planned to be without a real (or realistically faked) SFTP/SSH/Postgres/MySQL server or a real Windows Task Scheduler: the transport/dump-client adapters themselves (`sftpAdapter.ts`, `sshAdapter.ts`, `postgresDumpClient.ts`, `mysqlDumpClient.ts`), the strategy executors that wire them up (`fetchExistingExecutor.ts`, `remoteDumpExecutor.ts`, `directDumpExecutor.ts`), the connection testers, and `windowsTaskScheduler.ts`'s actual `schtasks.exe` calls — those are validated by the manual end-to-end runs against real infrastructure documented in this file's history, which remains the standard for verifying any change to them. `engine-cli` has no tests of its own yet.

Building this test suite found one more real (minor) bug, now fixed: `postgresCustomValidator`'s "PG_RESTORE_PATH not configured" branch populated only `warnings`, not `details` — so a `Failed` run's `error_message` showed a generic "Validation failed." instead of the actual reason. Every other failure branch in that validator already set `details`; this one was just missed.

## What this app does

A local, internal, "extremely reliable" tool that centralizes database backups across multiple clients/servers. Core pipeline:

**connect to servers → fetch DB backups → store locally → validate → apply retention → show status clearly.**

It is explicitly **not** responsible for server administration or deploys. Scope is deliberately narrow — do not add these until asked (they're called out in the spec as out of scope for v1): file/upload backups, S3, FTP, Google Drive, full automatic restore, VPS administration, general server monitoring, external notifications, multi-user support, cloud sync, mobile app. Adapters should still be designed so these can be added later without rewriting the engine — but don't pre-build them.

## Mandated development approach (per project.md "Primera etapa")

The spec explicitly says: **do not build the whole app at once**, and the user wants to be **contradicted** when a technical decision in the spec doesn't hold up, not just agreed with. This governed the architecture phase (already done — see "Architecture" below) and still governs what comes next: **no feature beyond the currently-agreed increment without explicitly checking in first.** All three backup strategies, retention, an automated test suite for `engine-core`, connection testing, config export/import, and Task Scheduler registration are done; don't start on the GUI without asking (or on packaging) — each is a deliberate, separately-agreed increment, not a natural continuation to pick up unprompted. (This has held in practice across every increment so far — architecture, `fetch_existing`, `remote_dump`, `direct_dump`, retention, automated tests, connection testing + config export/import, Task Scheduler registration — each chosen via an explicit question or explicit request from the user before starting.)

## Distribution constraints (non-negotiable, shape the architecture)

The end product must be a distributable Windows executable/installer for non-technical daily use:

- Produces a `.exe` and preferably a Windows installer.
- Must **not** require Node.js, npm, Rust, or any dev tooling on the target machine.
- Launches from Start Menu / shortcut like a normal app.
- Config, SQLite DB, and logs persist under AppData or a configurable directory — **never inside the install folder** (so updates don't wipe them).
- Must support future app updates without losing configuration or history.
- The backup engine must be able to run **unattended via Windows Task Scheduler even when the GUI is closed** — avoid designing this as "always keep our own background process running" if Task Scheduler integration can do it.
- Packaging must bundle or correctly resolve whatever is needed to validate dumps (e.g. `pg_restore`) — do not assume PostgreSQL is installed globally on the target machine.

## Preferred stack (propose deviations, don't silently swap)

- React + TypeScript for the UI, Tailwind CSS, HeroUI. **First screen (Dashboard) built** — see "UI" below. Versions used are deliberately bleeding-edge, from `pnpm create vite@latest` at the time: Vite 8.2.x, React 19.2.8, TypeScript ~6.0.2, Tailwind CSS v4 (`@tailwindcss/vite` plugin, CSS-first `@import` — no `tailwind.config.js`), HeroUI **v3** (`@heroui/react` 3.2.4 — a from-scratch rewrite of the older NextUI-based v2, compound components via `Object.assign`, built on React Aria, `onPress` not `onClick`, no `<Provider>` wrapper needed).
- Tauri as the desktop shell, if it proves adequate for the constraints above — as a "thin shell": Rust limited to boilerplate + OS-integration plugins (single-instance, shell/sidecar, dialog, opener, later autostart/updater), never business logic or direct SQLite access. **Started 2026-08-24 — see "Desktop shell (Tauri)" below.**
- SQLite for configuration, history, and logs — implemented via `better-sqlite3` in `engine-core` (WAL journal mode, `busy_timeout=5000`, `foreign_keys=ON`; see `db/connection.ts`).
- Backup engine logic kept separate from the UI, and runnable headless — implemented: `engine-core` has zero UI/Tauri dependency, and `engine-cli` runs it from a plain terminal.
- Avoid unnecessary dependencies. Current engine-core runtime deps: `ssh2-sftp-client` + `ssh2` (SFTP/SSH transports), `@napi-rs/keyring` (Windows Credential Manager — the maintained keytar replacement), `pino` (file logging). No new npm dependency was needed for `direct_dump` — it shells out to local `pg_dump`/`mysqldump` binaries via `node:child_process`, not a DB client library. `engine-cli` adds `commander`. Eventual packaging is expected to use `@yao-pkg/pkg` to compile `engine-cli` to a standalone exe — not done yet.

## Architecture: three separate concepts, not two

The spec originally framed this as just "connections" (SFTP/SSH), but the implemented model deliberately separates three concerns so a third strategy can be added later without reshaping the schema or the orchestrator:

- **Backup strategy** (`backup_tasks.strategy`, `BackupStrategyKind` in `engine-core/src/types.ts`): `fetch_existing` | `remote_dump` | `direct_dump`. Decides the overall pipeline shape. The orchestrator (`engine-core/src/orchestrator/runBackupTask.ts`) depends only on the `BackupStrategyExecutor` interface (`engine-core/src/strategies/types.ts`), never on a transport or DB-connection type directly.
- **Transport** (`transports` table, `engine-core/src/transports/`): `sftp` | `ssh`. How bytes move between a remote host and this PC. Used only by `fetch_existing` (sftp) and `remote_dump` (ssh).
- **Database connection** (`database_connections` table, `engine-core/src/databaseConnections/types.ts`): real DB credentials (engine, host, port, database, username, `password_secret_ref`). Used only by `direct_dump`. `fetch_existing`/`remote_dump` deliberately never need DB credentials.

Status of each strategy:

- **`fetch_existing` — implemented** (`strategies/fetchExistingExecutor.ts`, uses `transports/sftpAdapter.ts`): SFTP-only. Connect → list remote files → diff against `Success` runs by filename+size (not remote mtime — see below) → download newest candidate, hashing incrementally while streaming → return a `ProducedDump`. When nothing new is found it throws `NoNewDumpAvailableError`, which the orchestrator treats as a successful no-op run, not a `Failed` one — never surface "already up to date" as an error. Every invocation of the *next* strategy legitimately produces a fresh file, so this "already downloaded" concept is specific to `fetch_existing`.
- **`remote_dump` — implemented** (`strategies/remoteDumpExecutor.ts`, uses `transports/sshAdapter.ts` via raw `ssh2` — not `ssh2-sftp-client`, because it needs both `.exec()` and `.sftp()` on one connection): connect → run `transports.remote_command` → non-zero exit code fails the run → resolve the expected file path via `transports.remote_output_path_template` (supports a `{date:YYYYMMDD_HHmm}`-style token, resolved in `transports/outputPathTemplate.ts` — deliberately minimal, no heuristic "find the newest file" detection per the architecture plan's risk list) → download, hashing incrementally → if `transports.remote_cleanup` is set, delete the remote file **only after** a non-empty download succeeds (matches the spec's own step ordering: verify transfer, then optionally clean up). Gotcha already hit and fixed: `ssh2`'s raw `Client` is a plain `EventEmitter` — an unhandled `'error'` event (e.g. `ECONNRESET` after a rejected host key) crashes the whole process unless a permanent no-op listener is attached (see the comment in `sshAdapter.ts`); `ssh2-sftp-client` avoids this itself via a constructor `error` callback, which `sftpAdapter.ts` also sets.
- **`direct_dump` — implemented** (`strategies/directDumpExecutor.ts`, uses `databaseConnections/postgresDumpClient.ts` / `mysqlDumpClient.ts`): resolves the DB password from `SecretStore` via `database_connections.password_secret_ref`, shells out to `pg_dump --format custom` (postgres) or `mysqldump` (mysql) via `node:child_process.execFile`, writing straight to the local temp path — no transport, no download step. The DB password is passed via the `PGPASSWORD`/`MYSQL_PWD` environment variable to the child process, never as a CLI argument (argv is visible to other processes; env vars of a child process aren't). Filename is `{databaseName}_{YYYYMMDD_HHmmss}.{dump|sql}` (`.dump` for postgres, `.sql` for mysql) since there's no remote filename to reuse. **Licensing note, deliberately decided**: `mysqldump` is GPLv2 (unlike PostgreSQL's permissive license) — implemented dev-time-only via a `MYSQLDUMP_PATH` env var, same as `PG_DUMP_PATH`/`PG_RESTORE_PATH`; whether to bundle either binary into a future installer is a separate, still-open packaging decision.

**`direct_dump` tool version management — reviewed, deliberately not implemented, cheap groundwork laid.** The user flagged (2026-08-24) that `pg_dump`/`mysqldump` must never be assumed universally compatible with any server version, and asked for the architecture to be checked for blockers before this gets built for real. Findings from that review:

- **Already fine, no interface changes needed**: `DatabaseDumpClient.dump(config, localTempPath)` already receives the full connection config, which is everything a future implementation needs to internally detect the server version and pick a compatible tool — the encapsulation the user asked for ("`DatabaseDumpClient` debe encapsular toda esta lógica") is already how the interface is shaped, nothing to change there. `createPostgresDumpClient(pgDumpPath?)`/`createPostgresCustomValidator(pgRestorePath?)` already take the binary path as an overridable parameter (the env var is just the default), so a future per-server-version tool registry can resolve a path and pass it in with zero interface change. The orchestrator never touches dump-client selection — that's entirely inside `strategies/directDumpExecutor.ts` — so a future multi-version registry living there and in `postgresDumpClient.ts` already satisfies "without modifying the orchestrator." `ConnectionTestResult` and `DatabaseDumpClient`'s return shape can grow optional fields (`serverVersion`, `compatible`, etc.) later at zero cost. `app_settings` (existing key-value table) can already hold a future tool-path registry with no schema change. None of this was built now — it didn't need to be to avoid being blocked later.
- **One real blocker, fixed now because later is expensive**: `DatabaseEngine` only allowed `'postgres' | 'mysql'`, and the SQLite `CHECK` constraints on `database_connections.engine` / `backup_tasks.db_engine` enforced exactly that — no way to distinguish MariaDB from MySQL. SQLite can't alter a `CHECK` constraint in place; adding a value after real data exists needs a full table-recreate migration. Nothing has shipped yet, so widening it now is free. Fixed: `DatabaseEngine` is now `'postgres' | 'mysql' | 'mariadb'`, both CHECK constraints updated, and `databaseConnections/mariaDbDumpClient.ts` added as a stub (`dump()` throws "not implemented yet") wired into `directDumpExecutor.ts`'s dispatch — the exact same "declare the case now, stub the implementation" pattern already used for `remote_dump`/`direct_dump` themselves. `testDatabaseConnection.ts` routes `mariadb` through the existing MySQL connection tester (MariaDB is wire-compatible for a basic `SELECT 1` auth check) — that reuse does **not** extend to dump-tool selection, which is a separate question. Verified end-to-end: a `mariadb` database connection and task are creatable today, and running one fails cleanly with "mariadb direct_dump is not implemented yet." rather than a schema error or a crash.
- **Item (1), server/tool version *visibility*, implemented (2026-08-24)** — deliberately the smallest useful slice of the four, chosen directly by the user over the bigger ones. `ConnectionTestResult` gained optional `serverVersion`/`localToolVersion` fields (exactly the zero-cost growth point noted above). `postgresConnectionTester.ts` runs `SHOW server_version` (a clean bare version string, not `SELECT version()`'s full banner) after the existing `SELECT 1` check, plus `psql --version` for the local tool — both best-effort, neither ever fails the overall test if the lookup itself errors. `mysqlConnectionTester.ts` runs `SELECT VERSION()` (which, as this note already anticipated, doubles as the MySQL/MariaDB distinguisher — MariaDB's string includes the literal word "MariaDB") plus `mysql --version`. `lib/format.ts` gained `formatConnectionTestVersions()`, wired into every screen that already renders a connection-test result (`Dashboard.tsx`, `Tareas.tsx`, `Conexiones.tsx`). **This is version *visibility* only — not the compatibility gate in item (4) below, and nothing here blocks a task from running on a version mismatch.** Verified for real against this machine's local WAMP stack (not mocked): MySQL 9.1.0 returned `serverVersion: "9.1.0"`, and a separate MariaDB 11.5.2 instance on the same machine returned `"11.5.2-MariaDB"` — confirming the distinguisher works exactly as designed. PostgreSQL was not reachable on this machine at the time (no local install found), so that path is implemented and typechecks/builds cleanly but wasn't exercised against a live server this session — the same `psql`-based pattern was verified working in earlier sessions for the plain connectivity check, just not yet for this specific new version-query addition.
- **Item (3), real `mariadb-dump` support, implemented (2026-08-24)**, chosen directly by the user as the next slice. `mariaDbDumpClient.ts` went from a throwing stub to a real implementation, shelling out to `mariadb-dump` (dev-time path via new `MARIADB_DUMP_PATH` env var — **not** `MYSQLDUMP_PATH`; a MariaDB install ships both binaries, but they're genuinely different tools and the whole point of this increment was to stop treating them as interchangeable). **One real, concrete divergence found by hand, not assumed**: `mariadb-dump --help` on a real local install shows it does **not** support MySQL's `--ssl-mode=DISABLED|REQUIRED|VERIFY_IDENTITY` syntax at all — it uses an older boolean `--ssl`/`--skip-ssl` plus separate cert/key/CA flags. `mysqlDumpClient.ts`'s `SSL_MODE_FLAG` lookup table could not just be copied; `mariaDbDumpClient.ts` has its own `SSL_ARGS` mapping (`disable`→`--skip-ssl`, `require`→`--ssl`, `verify-full`→`--ssl --ssl-verify-server-cert`). `directDumpExecutor.ts`'s dispatch needed no changes — the stub/real swap was exactly the "declare the case now, stub the implementation" seam it was built with. `getSystemInfo.ts`'s tool-path diagnostics gained a distinct `MARIADB_DUMP_PATH` row (`MYSQLDUMP_PATH`'s label narrowed from "MySQL/MariaDB" to just "MySQL," since MariaDB has its own row now) — 145 vitest tests still pass (`getSystemInfo.spec.ts` updated to expect six tool env vars, not five). **Verified for real against this machine's local MariaDB 11.5.2** (not mocked): a genuine `mariadb-dump` run against the real `mysql` system schema produced a real ~2.5MB SQL file with the tool's own "MariaDB dump 10.19-11.5.2-MariaDB" banner and all 30 tables — confirmed it's really `mariadb-dump`'s own output, not `mysqldump` under a different name — and both the `disable`/`require` SSL flag mappings were separately exercised against the same real server without error.
- **Item (2), a pg_dump/pg_restore multi-version registry, implemented (2026-08-24)**, chosen directly by the user as the next slice. `databaseConnections/postgresToolRegistry.ts` (new) is exactly the `app_settings`-backed registry this note already anticipated: one JSON blob under a fixed key (`postgresToolRegistry`), mapping a PostgreSQL major-version string (`"18"`, `"15"`, or `"9.6"`-style for pre-10 releases — `extractMajorVersion()` handles both schemes) to a `{ pgDumpPath, pgRestorePath }` pair, with `list()`/`register()`/`unregister()`/`resolve(serverVersion)`. New CLI commands `pg-tools:register --pg-version <v> --pg-dump-path <path> --pg-restore-path <path>`, `pg-tools:list`, `pg-tools:unregister --pg-version <v>` (curl-equivalent-verified end-to-end via the CLI directly: register two versions, list both, unregister one, confirm it's gone). **Deliberately scoped to `pg_dump` selection only** — `postgresDumpClient.ts` was rewritten to an options-object constructor (`createPostgresDumpClient({ registry?, psqlPath?, defaultPgDumpPath? })`, fully backward-compatible with the old zero-arg call) that, only when *both* `registry` and `psqlPath` are supplied, runs `SHOW server_version` via `psql` before dumping and resolves a version-matched `pg_dump` from the registry, falling back to `defaultPgDumpPath` (i.e. `PG_DUMP_PATH`) whenever nothing is registered for that major version, the query fails, or no registry was wired in at all. `pg_restore`/`postgresCustomValidator.ts` deliberately still use the single `PG_RESTORE_PATH` unchanged — as this note already flagged, pg_restore's compatibility concern is matching the *dump's origin* version, not a live connection, a different problem not bundled into this pass. `directDumpExecutor.ts`'s `resolveDumpClient()` now takes an optional `settingsRepo` and only constructs a `PostgresToolRegistry` for the postgres case; `runBackupTask.ts`'s `RunBackupTaskDeps` gained an optional `settingsRepo` threaded through from `engine-cli`'s `buildContext()` at both call sites (`runTaskNow`/`run-due`) — omitting it (as every existing orchestrator test still does) preserves today's single-`PG_DUMP_PATH` behavior exactly. New `test/databaseConnections/postgresToolRegistry.spec.ts` (10 tests, real in-memory SQLite via `testContext.ts`, no mocking) covers `extractMajorVersion`'s pre-/post-10 edge cases and register/resolve/unregister round-tripping, including persistence across separate registry instances over the same `settingsRepo` — 155 vitest tests now pass. **Not live-verified against a real PostgreSQL server this session** — same gap already flagged for item (1)'s version-visibility work: no local PostgreSQL install exists on this machine, so the version-aware `dump()` path (as opposed to the registry CRUD itself, which *was* exercised for real via the CLI) is implemented and typechecks/builds cleanly but hasn't been exercised end-to-end against a live server.
- **Item (4), the pre-flight compatibility gate, implemented (2026-08-24)**, chosen directly by the user as the next slice after the registry. `databaseConnections/testDirectDumpCompatibility.ts` (new) combines three checks the existing `database-connection:test`/`task:test-connection` deliberately don't: connection+auth (delegates to `testDatabaseConnection`), the server's detected version, and whether a locally *usable* dump tool actually exists on disk for it — returning `{ ok, connection, toolPath?, toolCompatibility, message }` where `toolCompatibility` is `'registered'` (an exact postgres-registry major-version match — see the registry note above), `'default-unverified'` (no registry match, but the engine's default env-var tool — `PG_DUMP_PATH`/`MYSQLDUMP_PATH`/`MARIADB_DUMP_PATH` — is configured and the file exists; usable but never confirmed compatible with *this* server's version), or `'missing'` (the only state that fails the gate — no usable tool resolved at all, or the resolved path doesn't exist on disk). Deliberately permissive by design: a single-version install with no registry entries at all still passes as `'default-unverified'`, since the realistic common case (one pg_dump version, one server version, no need for a registry) shouldn't be blocked just because nothing was explicitly registered — the gate exists to catch "no usable tool" and "can't even connect," not to demand registry usage. Takes a `testConnectionOverride` test-only injection seam (mirrors `RunBackupTaskDeps.resolveExecutorOverride`'s role), letting `test/databaseConnections/testDirectDumpCompatibility.spec.ts` (8 tests, real settingsRepo/registry, real temp files for the "exists on disk" checks) exercise every `toolCompatibility` branch without a live DB server — 163 vitest tests now pass.
  - **New CLI command `task:test-compatibility <taskId>`** (direct_dump only — errors clearly for the other two strategies, which have no equivalent concept) prints the full result and exits nonzero on failure, mirroring `task:test-connection`'s shape.
  - **The actual gate enforcement point is `task:set-schedule`** (CLI) and `POST /tasks/:id/schedule` (serve) — both now run the compatibility check before ever enabling a direct_dump task's schedule, refusing with the full result and an explicit "re-run with `--force`" hint (CLI) or `409 { error: 'compatibility_failed', compatibility }` (HTTP) otherwise. A new `--force`/`force: true` escape hatch skips the check entirely — this is a local internal tool, not a system that should ever be unable to be overridden by the person running it. `POST /tasks` (inline schedule at task-creation time) applies the same gate but degrades more gracefully: if creating the schedule is blocked, the task itself is still created (only the schedule step is skipped), returned as a normal `201` with a `scheduleBlocked` field carrying the compatibility result — losing a whole task because its optional inline schedule failed a check would be worse than just leaving it unscheduled. The gate is skipped entirely (not evaluated at all) for `fetch_existing`/`remote_dump` tasks and whenever `--disable`/`scheduleEnabled: false` is being set, since disabling a schedule can never need a compatibility check.
  - **UI wired in the same increment**: `Tareas.tsx` and `ClienteDetalle.tsx`'s tasks tab both gained a "Probar compatibilidad" icon-button (a checkmark-circle icon, reusing the existing icon-button-plus-tooltip pattern) next to "Probar conexión," shown only for direct_dump tasks. `TaskCreateWizard.tsx` and `TaskEditModal.tsx` both handle a blocked schedule directly rather than just surfacing a generic error: the create wizard shows a dedicated follow-up screen after task creation ("la tarea se creó, pero el horario no pudo activarse") with "Continuar sin horario" and "Forzar y activar horario" actions; the edit modal shows the same warning inline with a "Forzar y guardar de todos modos" button. `tasksClient.ts` gained a `ScheduleCompatibilityError` class (thrown by `setTaskSchedule` on a `409`) so callers can catch it distinctly from a real request error, plus `CreatedTask` (the `BackupTask & { scheduleBlocked? }` shape `createTask` can now return).
  - **Verified end-to-end via CLI and curl** (no local PostgreSQL install exists on this machine, so this exercises the real "connection fails, PSQL_PATH unset" branch rather than a live version-match — the same caveat already on file for items (1)/(2)'s Postgres-specific paths): created a real direct_dump task, confirmed `task:test-compatibility` and `POST /tasks/:id/test-compatibility` both correctly report `ok: false`/HTTP `502`, confirmed `task:set-schedule --time 03:00` and `POST /tasks/:id/schedule` both correctly refuse (CLI nonzero exit, HTTP `409`), and confirmed `--force`/`force: true` on both correctly bypasses the gate and applies the schedule.
- **Equivalent registries for `mysqldump`/`mariadb-dump`, implemented (2026-08-24)**, chosen directly by the user as the next slice after the compatibility gate — closing the gap this note used to flag ("today only postgres has a version-keyed registry"). `databaseConnections/mysqlToolRegistry.ts` and `mariaDbToolRegistry.ts` (both new) mirror `postgresToolRegistry.ts`'s shape exactly (`app_settings`-backed, one JSON blob each under `'mysqlToolRegistry'`/`'mariaDbToolRegistry'`, `list()`/`register()`/`unregister()`/`resolve(serverVersion)`) but are deliberately **separate files, not a shared generic factory or a single cross-engine registry**: the value shape differs (postgres tracks a dump+restore pair; mysql/mariadb each track one dump path, since there's no separate "restore" tool in this app's model for them), and so does the version-key scheme — `extractMysqlMajorMinorVersion()`/`extractMariaDbMajorMinorVersion()` key by **major.minor** (e.g. "9.1", "11.5"), not postgres's single-number-from-v10-on scheme, since MySQL/MariaDB's own compatibility/support boundaries are drawn at the minor-series level (8.0 vs 8.4 vs 9.1; 10.11 vs 11.4 vs 11.5 are each distinct series). MariaDB's extractor doesn't need to special-case the `"-MariaDB"` suffix in a raw server-version string like `"11.5.2-MariaDB"` — the regex simply stops at the first non-digit after the minor version. `mysqlDumpClient.ts`/`mariaDbDumpClient.ts` got the same treatment `postgresDumpClient.ts` got in the earlier increment: rewritten to an options-object constructor (`{ registry?, mysqlPath?, defaultMysqldumpPath?/defaultMariaDbDumpPath? }`, fully backward-compatible with the old zero-arg call) that, only when both `registry` and `mysqlPath` are supplied, runs `SELECT VERSION()` via the `mysql` CLI client before dumping and resolves a version-matched tool, falling back to the default env var whenever nothing matches. Both reuse `MYSQL_CLI_PATH` for version detection (not a MariaDB-specific client path) — the same reuse `testDatabaseConnection.ts` already established for connectivity/version checks, since MariaDB is wire-compatible with the `mysql` client for querying even though `mariadb-dump` itself is a genuinely different binary from `mysqldump`. `directDumpExecutor.ts`'s `resolveDumpClient()` now wires a registry in for all three engines when `settingsRepo` is present, not just postgres. `testDirectDumpCompatibility.ts` now dispatches to the correct engine's registry (previously it only ever checked postgres's, so mysql/mariadb could never return `'registered'` — dead code path until now). New CLI commands `mysql-tools:register --mysql-version <v> --mysqldump-path <path>` / `mysql-tools:list` / `mysql-tools:unregister`, and the equivalent `mariadb-tools:*` trio. New `test/databaseConnections/mysqlToolRegistry.spec.ts` (9 tests) and `mariaDbToolRegistry.spec.ts` (9 tests), plus two new cases in `testDirectDumpCompatibility.spec.ts` covering the mysql/mariadb `'registered'` branch — 183 vitest tests now pass. **Verified for real against this machine's real MySQL 9.1.0 and MariaDB 11.5.2 instances (not mocked), more thoroughly than postgres's registry could be** (no local PostgreSQL install exists on this machine — see the postgres registry note above): registered each engine's real dump binary, confirmed `task:test-compatibility` correctly flips from `'missing'`/`'default-unverified'` to `'registered'` after registering, and — going further than the postgres verification could — ran a real `task:run` for both engines with `MYSQLDUMP_PATH`/`MARIADB_DUMP_PATH` deliberately pointed at a nonexistent path, confirming each dump still succeeded (`Success`, real multi-MB `.sql` files with correct checksums) by actually using the registered version-matched tool instead of the broken default — definitive proof the registry-aware `dump()` path, not just the compatibility-gate's connectivity check, really works end-to-end.
- **UI for managing the three tool registries, implemented (2026-08-24)**, prompted directly by the user asking whether the app itself could register a tool given just its file location, instead of the CLI-only `pg-tools:*`/`mysql-tools:*`/`mariadb-tools:*` workflow. Landed on the Configuración screen, right below the existing read-only tool-path diagnostics table, as the natural home (same domain: direct_dump dump-tool paths). Required new `serve` endpoints — `GET /tool-registry` (returns all three engines' registries in one call) and `POST /tool-registry/:engine/register` / `.../unregister` (`:engine` is `postgres`/`mysql`/`mariadb`; register validates the engine-appropriate required fields — `pgDumpPath`+`pgRestorePath` for postgres, one path for mysql/mariadb — and 400s with a clear message if missing) — reusing the existing three registries unchanged, no engine-core changes needed. `Configuracion.tsx` gained one shared `ToolRegistrySection` component (table of registered versions + an add-row form), instantiated three times with a different `fields` list per engine — a deliberate exception to this app's usual "don't share generic UI across engines" precedent (dump *clients* stay separate per engine since their actual dump/SSL logic differs; this component only differs in which path fields exist, so collapsing it avoided three ~90-line structurally-identical blocks). The add-row form's path field(s) get the same native-file-picker treatment as "Carpeta local" in Clientes.tsx (`@tauri-apps/plugin-dialog`'s `open({ directory: false, filters: [...] })`, gated behind `isTauri()`, degrading to a plain text input in the browser-only dev workflow) — reuses the already-granted `dialog:default` capability, no new Tauri permission needed since file-open and directory-open are the same plugin API. Version is still a free-text field the user types (e.g. "9.3", "18") rather than auto-detected from the picked binary — kept simple and consistent with the CLI, which already requires this. Verified end-to-end via curl against the real `serve` endpoints (register/list/unregister for all three engines, and the 400 on a missing required field) before wiring into the UI; the rendered form itself wasn't screenshot-confirmed (same standing caveat as other UI work this session — no browser tool available to Claude in this environment).
- **Real future work, still not started, don't build without asking**: auto-downloading the right `pg_dump`/`mysqldump`/`mariadb-dump` version on demand (the way DBeaver fetches JDBC drivers) — feasible in principle for Postgres (EDB publishes versioned client-only ZIPs at stable URLs) and MariaDB (similarly simple direct downloads), murkier for MySQL specifically (Oracle's distribution isn't as scriptable, plus the still-open GPL bundling question) — raised and explicitly deferred the day item (1) was built, meaningfully bigger than any of these slices, not started.

**Checksum responsibility split**: `ProducedDump.checksumSha256` is optional. A strategy populates it only if it can compute SHA-256 during `produce()` at no extra read cost (`fetch_existing`/`remote_dump` do, while streaming the download). If left `undefined`, the orchestrator hashes the temp file once itself as a fallback — `direct_dump` is the real case that exercises this path, since `pg_dump`/`mysqldump` write straight to disk via subprocess redirection with no stream available to hash incrementally. Either path hashes exactly once — never twice, never on a re-run of an already-`Success` file.

**Database engine validators** (`engine-core/src/validators/`): PostgreSQL (`postgresCustomValidator.ts`, shells to `pg_restore --list` via a dev-time `PG_RESTORE_PATH` env var — now vendored for real installs, see "Packaging"), MySQL/MariaDB (`mysqlDumpValidator.ts`, added 2026-08-24 — see below), and a generic exists/size>0 check (`genericValidator.ts`) used for everything else.

**`mysqlDumpValidator.ts`, implemented (2026-08-24)**, closing the "no MySQL-specific validator" gap this note used to flag. mysqldump's plain-SQL output has no binary structure `pg_restore --list` can lean on, but both `mysqldump` and `mariadb-dump` consistently write two textual markers — confirmed by hand against this machine's real MySQL 9.1.0 and MariaDB 11.5.2 instances, not assumed from docs: a `-- MySQL dump`/`-- MariaDB dump` banner as the first real line, and a literal `-- Dump completed on <date>` footer as the *last* line, written only once every table has finished dumping successfully. Checking for that footer is the actual value here: a process killed mid-dump, a full disk, or a dropped connection can all still leave a large, well-formed-looking, non-empty SQL file that the generic exists/size>0 check would wave through — the footer's absence is a real, specific truncation signal that check can't provide. Reads only the first/last ~512 bytes (not the whole file) via `fs.promises.open()` + partial `read()`, so this stays cheap even for a multi-GB dump. Wired into `runBackupTask.ts`'s `pickValidators()` for both `dbEngine: 'mysql'` and `'mariadb'` (one validator covers both, since the completion-footer format is identical for both tools) — `DumpValidator.engine`'s type also picked up `'mariadb'` as a value in the same pass, a small pre-existing staleness (the field is purely descriptive, never used for dispatch, so this was harmless but worth fixing while already there). New `test/validators/mysqlDumpValidator.spec.ts` (6 tests, using real captured mysqldump/mariadb-dump output as fixtures, not synthetic text) — 189 vitest tests now pass. **Verified end-to-end for real**: ran an actual `direct_dump` task against this machine's real MySQL 9.1.0, confirmed the run completed `Success` with the new validator exercised (not mocked) alongside the generic one.

## Security requirements

- Never store SSH private keys inside SQLite — store paths/references only.
- Never store passwords in plaintext; secrets are DPAPI-encrypted at rest — see "Secrets storage" below for what that means today (it changed once, deliberately, from the original plan).
- Never log credentials.
- Validate SSH host fingerprints; don't silently accept unknown hosts — support registering/confirming known hosts.
- Principle of least privilege: a backup user should not need root/admin on the remote server.

## Secrets storage

**Migrated 2026-08-24** from Windows Credential Manager (`@napi-rs/keyring`, CurrentUser-scope DPAPI under the hood) to a `secrets` table in this app's own SQLite database, encrypted with **LocalMachine-scope DPAPI** via `@primno/dpapi` (`secrets/machineDpapiStore.ts`, `MachineDpapiSecretStore`) — a real, user-initiated architecture change, not a bug fix, triggered by hands-on pain registering a real Windows Scheduled Task (see "Scheduler" below).

- **Why**: Credential Manager's CurrentUser-scope DPAPI meant only the specific Windows account that saved a secret could ever decrypt it again — which meant a Scheduled Task had to run *as that interactive user*, which meant `scheduler:install` needed that user's real Windows account password. The user hit this directly (a genuinely wrong-looking "usuario o contraseña incorrectos" error from `schtasks.exe` that turned out to be a PowerShell double-quote/`$`-interpolation issue, not a wrong password) and asked how other backup software avoids this. Answer, researched and confirmed: professional tools mostly run as a Windows Service (not a per-task Scheduled Task) and use either LocalMachine-scope DPAPI or their own master-password-derived key, precisely to decouple "can decrypt this secret" from "which specific human is logged in." LocalMachine-scope DPAPI was the smaller of the two changes and reused this app's existing `SecretStore` interface unchanged, so that's what got built.
- **What changed structurally**: `secrets` table added (`db/migrations/0003_add_secrets_table.sql` — `ref TEXT PRIMARY KEY, ciphertext BLOB`). `MachineDpapiSecretStore` implements the exact same `SecretStore` interface (`get`/`set`/`delete`) `WindowsCredentialManagerStore` did, so every caller (transports, database connections) needed zero changes. `@napi-rs/keyring` dependency removed entirely — nothing else used it.
- **What this unblocked**: `scheduler:install` now registers the task's Principal as SYSTEM (well-known SID `S-1-5-18`, no `LogonType`/password at all — see "Scheduler") since LocalMachine-scope DPAPI is decryptable by *any* account on the machine, SYSTEM included. This is the real payoff: a client install can now register scheduled backups with **zero password prompt**, which the original interactive-user design could never have offered a non-technical user cleanly.
- **A second, necessary fix this forced**: `paths.appDataDir()` (the SQLite DB + logs location) resolved via `%APPDATA%`, which is *per-user* — a SYSTEM-run process has its own, different, empty `%APPDATA%`, so it would never have found the real database at all. Moved to `%PROGRAMDATA%` instead (`C:\ProgramData\CodebiusBackupManager\`), Windows' own documented location for machine-wide data independent of which account is running — this would have silently broken every scheduled run otherwise, SYSTEM or not. `defaultBackupsRootDir()` (the UI's suggested-default new-client folder) moved the same way, for the same underlying reason plus a second one: a backup folder living inside a specific user's profile is exactly the kind of path a SYSTEM-run process might not have ACL access to, so the suggested default no longer points there at all.
- **SSH private keys now get copied into this app's own storage at creation time, not just at config-import time.** `transports/copyPrivateKey.ts` (`copyPrivateKeyIntoAppStorage`) — called from `transport:create-sftp`/`-ssh`/`:update` and their `serve` HTTP equivalents, right before the repo call — copies whatever file the user pointed at into `paths.keysDir()` (`%PROGRAMDATA%\CodebiusBackupManager\keys\<uuid>.key`, the same directory `config:import`'s restored keys already used — that helper's directory function was renamed `paths.importedKeysDir()` → `paths.keysDir()` to reflect it's no longer import-only) and stores *that* path on the transport. Same reasoning as the ProgramData move: a key file sitting wherever the user originally put it (their Downloads folder, another account's profile) isn't guaranteed readable by SYSTEM or guaranteed to still exist later; owning a copy sidesteps both problems.
- **Verified for real, not just unit-tested**: created a real transport with a real key file and a real passphrase through the actual running HTTP API, confirmed the key was copied with byte-identical content, confirmed the `secrets` table held real ciphertext (not plaintext) at the expected size, and confirmed `MachineDpapiSecretStore.get()` decrypted it back to the exact original passphrase — all against this machine's real DPAPI, not a fake. 145 vitest tests now pass (`machineDpapiStore.spec.ts` — 6 tests using real DPAPI calls, self-skips on non-Windows via `isPlatformSupported`; `copyPrivateKey.spec.ts` — 3 tests with real temp files).
- **`redactPassword`/the PIN-vs-password error hint built earlier the same day are now dead code, deleted rather than kept "just in case"**: there is no password anywhere in the scheduler flow anymore, so nothing to redact and no wrong-credentials error mode to hint about — the problem was eliminated at the root instead of just getting a better error message for it.

## Local storage layout

Conceptual path: `Backups/{cliente}/{database}/{YYYY}/{MM}/`, e.g. `D:\CodebiusBackups\Winners\postgres\2026\08\winners_2026-08-23_0300.dump`.

The filename is never the source of truth for metadata — SQLite is (`backup_runs` table). Track per backup: client, database, server, remote timestamp, download timestamp, size, hash/checksum, status, local path, method used, duration, errors.

Implementation note: the `{database}` path segment isn't a separate schema field for `fetch_existing`/`remote_dump` tasks (only `direct_dump` has a real database name, via `database_connections`), so `resolveTargetDir()` in `orchestrator/runBackupTask.ts` uses a slugified version of the task's own name instead. Revisit if that ever reads confusingly in the UI.

## Validation

A downloaded file is never automatically considered a valid backup. Minimum checks: file exists, size > 0, transfer fully completed, checksum where possible. For PostgreSQL custom-format dumps, validate with `pg_restore --list`. (Automated test-restores are a later goal, not a v1 requirement.)

Status values: `Pending`, `Running`, `Producing`, `Validating`, `Success`, `Warning`, `Failed`. (The spec originally said `Downloading` — renamed to `Producing` since it must read correctly for all three strategies, not just ones that transfer a file.)

## Retention

**Implemented** (`engine-core/src/retention/applyRetention.ts`), applied by the orchestrator after every completed attempt of every strategy — regardless of that run's own outcome (an old backup can be past policy even if today's run failed), including the `fetch_existing` no-op "already up to date" case. Configurable per client (`clients.retention_count`/`retention_days`, set via `client:create --retention-count`/`--retention-days`) with an optional per-task override (`task:create --retention-count`/`--retention-days`); neither set means no retention for that task.

Two hard invariants, enforced regardless of how aggressive the configured policy is: a run is only ever a delete candidate if a **later `Success` run with an actual file** exists (the newest is never touched), and **at least one real backup always survives**. If both `count` and `days` are configured, a run must violate *both* to be deleted (the more conservative merge, not more aggressive). Every deletion is recorded in `retention_deletions` (`retention:history <taskId>` to inspect) and is skipped on all future passes via `RetentionDeletionsRepo.listDeletedRunIds` — without that exclusion, an already-pruned run gets silently re-evaluated (and re-recorded as newly "deleted," since unlinking an already-gone file just throws ENOENT) on every subsequent run forever, which is a bug that was actually hit and fixed while building this.

**Gotcha already hit and fixed, worth knowing if you touch this code**: `RunsRepo.listSuccessfulRuns` filters to `status = 'Success' AND local_path IS NOT NULL` specifically to exclude `fetch_existing`'s no-op "no new backup needed" runs (which are legitimately `Success` but have no file). Without that filter, a no-op run — which is always the *newest* row — occupies the "never touch rank 0" protection slot while having nothing to protect, which pushed the one real backup down into "older, prunable" territory and defeated the survivor-floor invariant in testing: a single real backup got deleted, leaving zero, with several no-op Success runs sitting on top of it. If `listSuccessfulRuns` or the retention algorithm's rank/day logic changes, re-verify this exact scenario (one real Success backup, several subsequent no-op Success runs, an aggressive policy) before considering it safe.

Retention only ever considers `Success` runs "valid" — `Warning`-status backups (which do have real files) are deliberately left alone entirely, not counted and never deleted, erring conservative for files a human may not have reviewed yet. The "age" used for `retention_days` is `downloaded_at` (or the equivalent local produce time for `direct_dump`), never `remote_modified_at` — see the remote-mtime trust caveat elsewhere in this file.

## Connection testing

**Implemented** — spec's "Probar conexión" requirement, extended to cover all three strategies without ever producing a dump/download:

- **SFTP/SSH transports**: `TransportAdapter.testConnection()` (already existed) — connect, list/`echo`, disconnect. Exposed directly via `transport:test <transportId>`, or per-task via `task:test-connection <taskId>`.
- **Database connections** (`direct_dump`, new): `databaseConnections/testDatabaseConnection.ts` resolves the password from `SecretStore` and dispatches to `postgresConnectionTester.ts` (`psql --command "SELECT 1"`) or `mysqlConnectionTester.ts` (`mysql --execute "SELECT 1"`) — a trivial query that proves connectivity + auth without invoking `pg_dump`/`mysqldump` at all. Dev-time binary paths via `PSQL_PATH`/`MYSQL_CLI_PATH` env vars (distinct from `PG_DUMP_PATH`/`MYSQLDUMP_PATH` — different binaries, same bin directory). Exposed via `database-connection:test <id>`, and `task:test-connection` now dispatches to this automatically for `direct_dump` tasks (closing the gap this file used to note here).
- **Client-level aggregate**: `client:test-connections <clientId>` runs every transport's and every database connection's test for that client in one shot and reports each result — this is the direct answer to "test a client's connection without downloading backups/dumps."

Both transport and database-connection testers return the same `ConnectionTestResult` shape (`{ ok, message, latencyMs? }`), reused rather than duplicated.

## Configuration export/import

**Implemented** (`engine-core/src/config/`) — `config:export [--client <id>... | --all] [--output <file>]` and `config:import --file <path>`.

**Secrets are never exported**, by design: SSH key passphrases and DB passwords live only in Windows Credential Manager on the machine that created them, and a `secret_ref` exported to a portable file would be meaningless (or a real risk) elsewhere. Exported transports/database connections carry `hasPassphrase`/`hasPassword` booleans instead of any secret value or ref. After import, `ImportConfigResult.secretsNeedingReentry` lists (in plain language) every transport/database connection that needs its secret set again via the normal `--passphrase`/`--password` flags — this is a real, expected step in the import workflow, not an oversight.

Import **always creates new rows** — it never overwrites or merges into an existing client. A client whose name already exists fails with a clear per-client error (surfaced in that client's `errors` array) without aborting the rest of the batch; rename or remove the conflicting client first to re-import it. Within one client, transports/database connections/tasks are matched up by **name**, not by raw database id (ids are meaningless across a different SQLite file) — a task's `transportName`/`databaseConnectionName` is resolved against what was just (re-)created for that same client during the same import call.

**Bug fixed (2026-08-24)**: a task's schedule (time, enabled state, and now frequency/days-of-week/day-of-month) was exported into `ExportedTask` but **never actually restored on import** — `importOneClient()` created tasks via `createFetchExisting`/`createRemoteDump`/`createDirectDump` and simply never called `setSchedule()` afterwards, so every imported task silently came back unscheduled regardless of what the export said. Found while adding non-daily schedule frequencies (see "Scheduler" below) and fixed the same day: `importConfig.ts` now calls `deps.tasksRepo.setSchedule()` right after creating a task, if `task.scheduleTime` is set. Regression-tested (`exportImportConfig.spec.ts`) and curl/CLI-verified end-to-end with a throwaway weekly-scheduled task exported from one temp database and imported into another, confirming the restored task's schedule fields matched exactly.

**SSH private key content export/import — implemented (2026-08-24)**, closing the gap flagged earlier the same day. `ExportedTransport` gained `privateKeyContentBase64: string | null` — `exportConfig.ts` reads the key file at `transport.privateKeyPath` and base64-encodes it, leaving it `null` (graceful degradation, doesn't fail the export) if the file can't be read at export time. `importConfig.ts` writes that content to a fresh file under `paths.keysDir()` (`%PROGRAMDATA%\CodebiusBackupManager\keys\<uuid>.key` as of the 2026-08-24 secrets-storage migration below — was `importedKeysDir()`/`%APPDATA%` at the time this was first built, same day; `ImportConfigDeps.importedKeysDir` overridable for tests) and uses *that* path for the newly-created transport — `privateKeyPath` in the export is now informational only (the source machine's own path), never reused directly. When content is missing, the importer falls back to exactly today's old behavior (keeps the source path, which is almost certainly wrong on a different machine) and adds a `secretsNeedingReentry` entry telling the user to copy the file manually — reusing that existing array/reporting mechanism rather than inventing a parallel one, even though a private key file is a different kind of secret material than the passphrase/password refs it originally covered. **Security note, deliberately accepted**: this means an export file can now contain actual private key bytes (base64-embedded) for any transport whose key was readable at export time — unlike passphrases/passwords, which never leave this app's own encrypted secret storage (Windows Credential Manager at the time this was written; migrated to LocalMachine-scope DPAPI later the same day, see "Secrets storage" — the export/import boundary itself is unaffected by that migration, since it was never involved). A config export must now be handled as sensitive if any of its transports have private keys, which it wasn't before this change. Regression-tested with real temp files (`exportImportConfig.spec.ts`, `withTempDir` — content round-trips correctly, missing-file fallback still flags manual copy, `importedKeysDir` override works), and manually verified with real key files across two separate throwaway app-data directories (simulating two machines) before/after this session's automated tests — decoded content matched byte-for-byte.

## Scheduler

**Implemented** (`engine-core/src/scheduler/`) — no persistent background process of our own; each task gets its own real Windows Scheduled Task.

- **Per-task schedule**: `backup_tasks.schedule_time` (24h `"HH:MM"`, validated) + `schedule_enabled`, set via `task:set-schedule <taskId> --time HH:MM` / `--disable`. Deliberately per-**task**, not one global time — a client can have multiple tasks (different databases, different strategies) that legitimately need different times; the spec's literal wording ("una hora diaria") was read as "each task gets *a* daily time," not "the whole app gets one."
- **Non-daily frequencies, added 2026-08-24** (`backup_tasks.schedule_frequency`: `'daily' | 'weekly' | 'monthly'`, plus `schedule_days_of_week` — CSV of 0=Sunday..6=Saturday, and `schedule_day_of_month` — 1-31): requested directly by the user ("que el periodo de las tareas no sea unicamente diario? por ejemplo semanal, o determinado dia"). Added via `db/migrations/0002_add_task_schedule_frequency.sql` (an `ALTER TABLE ADD COLUMN`, not a rewrite of `0001_init.sql` — the latter would silently skip already-migrated databases, including the dev/demo one) rather than a schema rewrite, since real dev/demo data already existed; existing rows default to `'daily'` with no days configured, preserving prior behavior exactly. Validated in `tasksRepo.ts`'s `setSchedule()` (weekly requires a non-empty `scheduleDaysOfWeek`; monthly requires `scheduleDayOfMonth` 1-31; switching back to `'daily'` drops both), not via a SQLite `CHECK` — same "app-level invariant" precedent as the strategy/transport type matching. A `scheduleDayOfMonth` beyond the current month's length (e.g. 31 in April) clamps to that month's last day rather than silently never firing, in `isTaskDue.ts`. **Deliberately did not touch `taskDefinitionXml.ts`/`windowsTaskScheduler.ts`**: the Windows-side `CalendarTrigger` still fires **daily** at `schedule_time` regardless of frequency — `isTaskDue()` is what actually decides whether today matches, so a weekly/monthly task's OS trigger is just a daily nudge to check, same "OS trigger vs. code gate" split the LogonTrigger catch-up path already relies on. This kept the Task Scheduler XML/registration code (already tested, already the most fragile part of this subsystem) completely unchanged. `config export/import` was deliberately left alone too — it doesn't restore *any* schedule fields on import today (a pre-existing gap, not something this increment introduced or was asked to fix).
- **`isTaskDue(task, now, latestRun)`**: due only if scheduling is enabled, the task is active, today matches the configured frequency (every day for `daily`; one of `scheduleDaysOfWeek` for `weekly`; the clamped `scheduleDayOfMonth` for `monthly`), **no attempt of any status (Success/Warning/Failed) has happened yet today**, and the current time is at/after `schedule_time`. That "no attempt yet today" condition — not the OS trigger that fired — is what actually prevents a double-run; see below.
- **`runDueTasks`**: evaluates a list of tasks with `isTaskDue`, runs the due ones via the normal `runBackupTask`, and isolates failures per task (both a handled `Failed` run *and* an exception thrown before `runBackupTask` even creates a run row, e.g. a bad client reference) — the same "one client's failure must never stop the others" principle already applied everywhere else, applied here too.
- **One Windows Scheduled Task per backup task** (`scheduler:install <taskId>`), not one global poller — this gives exact OS-level triggering at each task's own configured time without needing to poll frequently. Each has **two triggers**: a `CalendarTrigger` at `schedule_time`, and a `LogonTrigger` (delayed 2 minutes) as a catch-up path for a PC that was off at the scheduled time. Both invoke `engine-cli run-due --task <taskId>` — never `task:run` directly — so `isTaskDue`'s guard is always what decides whether anything actually happens, which is exactly what makes it safe for both triggers to exist on the same task without risking a redundant same-day run.
- **Runs as SYSTEM, not the interactive user — a real architecture change made 2026-08-24, see "Secrets storage" below for the full story.** `scheduler:install` no longer takes `--password`/`--username` at all: there is no Windows password anywhere in this flow anymore. The trade-off is that *registering* (and, discovered the same day, *querying the status of*) a SYSTEM-owned task requires the calling process itself to be running elevated (as Administrator) — a real installer already runs elevated by default, so this is a one-time cost at setup, not a per-task one, but it does mean `scheduler:install`/`scheduler:status` fail from an ordinary non-elevated terminal. `scheduledTaskStatus()` can't actually tell "not registered" apart from "registered, but you're not elevated so I can't check" — schtasks.exe gives the exact same error for both — so it separately checks elevation (`net session`) and sets `ranNonElevated: true` on a `false` result so the CLI can say "couldn't confirm," not lie and say "not registered."
- **The Task Scheduler XML's `<Principal>` needed the well-known SID, not the literal name** — confirmed by hand against a real Windows install, not just from documentation: `<UserId>SYSTEM</UserId>` combined with `<LogonType>ServiceAccount</LogonType>` was rejected outright by `schtasks.exe /Create /XML` with a schema-range error at that exact element. The combination that actually works: `<UserId>S-1-5-18</UserId>` (SYSTEM's SID) with `<RunLevel>HighestAvailable</RunLevel>` and **no** `<LogonType>` element at all — Task Scheduler infers the service-account logon from the well-known SID alone.
- **Dev-time only, by necessity**: the registered Action currently runs `process.execPath` (the current `node.exe`) against `process.argv[1]` (this script's own absolute `dist/index.js` path) — there's no compiled `engine-cli.exe` yet (packaging isn't built — see "Not yet built"). `scheduler:install` will need updating once that exists, to target the installed exe instead.
- `scheduler:uninstall <taskId>` / `scheduler:status <taskId>` manage/query the same task by name (`scheduledTaskNameForBackupTask`, grouped under an `arkode` folder in Task Scheduler's own UI — renamed from `CodebiusBackupManager` on 2026-08-24, see "Current state").
- **Verified fully end-to-end (2026-08-24), the real live registration included** — the one thing the previous password-based design had explicitly deferred ("run it yourself when ready") got done, on the very session that replaced the mechanism that made it sensitive in the first place: `scheduler:install` registered a real task from an elevated PowerShell with zero password prompt, `scheduler:status` (also elevated) confirmed `Ejecutar como usuario: SYSTEM` and both triggers present, and the task was uninstalled again afterward since it pointed at a dev-database task id.

## UI

**Started, two screens (Dashboard, Clientes) implemented** in `packages/ui` — a plain Vite/React web app, not yet wrapped in Tauri (no `packages/desktop-shell` yet — see "Not yet built"). Built this way deliberately: no Rust/MSVC toolchain is installed on this dev machine yet, so a pure web app pulls the actual UI work forward without blocking on that install; the plan is still to wrap this in Tauri later, not to ship a web app.

- **`engine-cli serve [--port 4287]`**: a minimal read-only local HTTP server (`node:http`, binds `127.0.0.1` only), one endpoint `GET /status` returning `getDashboardStatus()`'s JSON. CORS-open (`Access-Control-Allow-Origin: *`) so the Vite dev server (a different port) can call it — this is explicitly a **dev-time convenience**, pulled forward from the already-planned future architecture where the real UI talks to the engine through a Tauri-side bridge instead of an HTTP server; don't build more HTTP surface on top of this without confirming that's still the intended path.
- **`getDashboardStatus()`** (`engine-core/src/status/getDashboardStatus.ts`, exported from `engine-core`'s index, tested in `test/status/getDashboardStatus.spec.ts`) replaces `status --json`'s old inline row-building logic (the CLI `status` command now calls it directly too) and fixes a real gap: it distinguishes the **latest attempt's status** (must surface a fresh `Failed` immediately, even today) from the **latest run that actually has a file on disk** (`RunsRepo.getLatestWithFileByTask` — `local_path IS NOT NULL`, same "no-op run isn't a real backup" pattern already used in retention's `listSuccessfulRuns`) for `sizeBytes`/`checksumSha256`/`lastGoodBackupAt`. Without this split, a `Failed` or no-op-`Success` latest run would blank out a still-valid older backup's size/age instead of showing both facts at once.
- **`Dashboard.tsx`** polls `/status` every 20s, flags a row as a problem (red left border + tinted background) if its latest status is `Failed`/`Warning`/`NeverRun`, **or** its last good backup is older than 26 hours (a daily task whose last *attempt* technically "succeeded" a while ago but hasn't produced anything fresh lately is still worth flagging). Renders a plain semantic `<table>` styled with Tailwind + HeroUI's CSS custom properties (`var(--danger)`, `var(--muted)`, etc.) rather than HeroUI's own `Table` component — a deliberate scope call for this session: HeroUI v3's `Table` is built on react-aria-components' collection API, meaningfully more setup than this first screen's time budget justified. Revisit if/when other screens need sorting/selection.
- Dark mode is hardcoded on (`<html class="dark">` in `index.html`) — no light-mode/theme-toggle exists yet, matching the spec's "dark mode" direction with nothing to switch away from until asked.
- **Visually verified by the user** (2026-08-24, screenshot): dark theme, status chips, and problem-row red-left-border flagging all render correctly against the seeded 3-client test data (Winners=Success healthy, Compagnucci=Failed with stale old backup, Carena=NeverRun) — matches the intended dense/problems-stand-out UX direction. Claude itself still has no browser/screenshot tool in this environment; `pnpm --filter ui build` and dev-server HTTP checks were Claude's own verification, the visual confirmation came from the user.
- **Per-row actions implemented (2026-08-24)**: "Ejecutar ahora" and "Probar conexión" buttons per task, plus "Ver error" to expand the latest run's full error message inline when the latest status is `Failed`. This required widening `serve` from strictly read-only: it now also handles `POST /tasks/:taskId/run` and `POST /tasks/:taskId/test-connection`, both reusing the exact same helper functions (`runTaskNow`/`testTaskConnection` in `engine-cli/src/index.ts`) as the equivalent CLI commands rather than duplicating the logic — a run or connection-test failure returns a normal JSON body (200 for a completed-but-Failed run, 502 for a failed connection test) since the request itself succeeded; only an unknown task (404) or an unexpected exception (500) is a request-level error. `DashboardRow` gained `latestErrorMessage` (from `latestRun.errorMessage`) to support this. **Still out of scope for this increment, deliberately not built**: "ver backups" (belongs to a future Historial screen) and "abrir carpeta local" (the browser has no filesystem access — genuinely needs Tauri's `opener` plugin, not something a workaround should fake).
- **Clientes screen implemented (2026-08-24)**: list of active clients (name, description, local base path, retention policy, task count) with "+ Nuevo cliente" and per-row "Editar"/"Desactivar" (`window.confirm()` — soft-delete via `is_active = 0`, tasks/backups untouched); both create and edit open a `Modal` (see the redesign note below — create started as an inline panel, then moved to a modal to match edit, per explicit request). Required real backend additions, not just UI: `ClientsRepo` gained `update()` (patch semantics — only the fields you pass are changed; `null` explicitly clears a nullable field, `undefined`/omitted leaves it alone) and `deactivate()`, plus a friendly "A client named X already exists" error instead of a raw SQLite `UNIQUE constraint failed` message on both create and rename. New CLI commands `client:list`/`client:update`/`client:deactivate`. `serve` gained `GET /clients` (each row includes a computed `taskCount`), `POST /clients`, `PATCH /clients/:id`, `POST /clients/:id/deactivate` — the first endpoints on this server needing a JSON request body, which needed an `OPTIONS` preflight handler added (`Access-Control-Allow-Methods`/`-Headers`) since the browser's CORS preflight wasn't needed by the earlier no-body POST endpoints.
- **"Carpeta local" — native folder picker added 2026-08-24, now that the Tauri shell exists.** Originally a free-text field, deliberately: a browser's directory picker (`showDirectoryPicker()`) never exposes the real OS path to JavaScript — only an opaque handle, by design, for sandboxing — so a browser-side picker couldn't produce a usable value for `localBasePath`, which `engine-cli` (running in Node, outside the browser sandbox) needs as a real Windows path string. This was always expected to resolve once Tauri existed, and did: `Clientes.tsx`'s `ClientFields` now shows an "Elegir…" button next to the text input, calling `@tauri-apps/plugin-dialog`'s `open({ directory: true })`. Gated behind `@tauri-apps/api/core`'s `isTauri()` check — the button only renders when actually running inside the Tauri shell, so the plain browser dev workflow (`pnpm --filter ui dev` on its own, no Tauri) keeps working exactly as before with just the text input, no broken import or dead button. Required on the Rust side: `tauri-plugin-dialog` added to `Cargo.toml`/registered in `lib.rs`, plus a `"dialog:default"` permission in `capabilities/default.json` (Tauri v2's capability system denies a plugin's API by default until explicitly granted). Visually confirmed working by the user in the real native window. Same root cause as "abrir carpeta local" below — that one still needs `tauri-plugin-opener`, not built yet.
- **Reactivation added same day**: a "Mostrar inactivos" toggle on Clientes fetches `GET /clients?includeInactive=true` (backed by new `ClientsRepo.listAll()`) and shows deactivated clients dimmed with a "(inactivo)" label and a "Reactivar" button in place of Editar/Desactivar, calling the new `POST /clients/:id/reactivate` (`ClientsRepo.reactivate()`, `client:reactivate` CLI command). `serve`'s router was refactored to parse `req.url` into `pathname`/`searchParams` via `new URL()` instead of matching the raw string, since a query param would otherwise have broken the existing exact-match routes. `clientsRepo.spec.ts` is now 10 tests (77 total).
- **Bug fixed same day**: the UI's own `postTaskAction` helper (`statusClient.ts`) treated `res.status >= 500` as a hard request error, which incorrectly caught the 502 that `test-connection` deliberately uses for a legitimate "connection failed" result (contradicting the very comment next to that line). Surfaced by the user hitting "Probar conexión" and seeing a raw "Request failed: 502" instead of the connection-test's real message. Fixed to only treat 404/500 as request-level errors.
- **Visual redesign (2026-08-24)**, requested via a reference screenshot ("algo asi en estetica quiero además") of a generic SaaS-admin-style mockup: replaced the top-tab nav with a left `AppShell` sidebar (`App.tsx`/`AppShell.tsx`) — brand mark, then the real screens as enabled nav items, then the not-yet-built v1 screens as **visibly disabled placeholders** (grayed, `cursor: not-allowed`, no click handler) so the intended structure previews without pretending those screens exist. `AppShell`'s nav list mirrors `project.md`'s actual v1 screen set (Dashboard, Clientes, Conexiones, Tareas, Historial, Logs, Configuración — "Detalle de cliente" is a drill-down from Clientes, not its own sidebar destination, so it has no nav entry); the mockup's own nav labels ("Backups", "Reportes") were **not** copied verbatim since they aren't real screens in this app's spec. Dashboard gained a stat-card row (`StatCard.tsx` + small original inline-SVG icons in `icons.tsx` — no icon library dependency added) with real, honestly-computed numbers: distinct client count, total task count, `Success` runs within the last 24h (via `latestAttemptAt`/`ageInHours`), and the existing `problemCount`. Action buttons got a rounded-full "pill" treatment via HeroUI `Button`'s `className`/`style` passthrough — a shared `lib/pillStyles.ts` exports `primaryPillStyle` (a celeste→azul→magenta 3-stop gradient, inline `style` so it reliably overrides the variant's own background — tuned live from a "más azul de fondo"/gradient-reference-image request) and `dangerPillStyle`, reused across Dashboard/Clientes/Conexiones rather than duplicated per file. "Mostrar inactivos"/"Mostrar inactivas" became a real toggle switch (`Switch.tsx`, plain `role="switch"` button — not HeroUI's own compound `Switch`, same "plain HTML now, revisit later" call already made for `Table`/`Modal`) instead of a button that relabels itself. The dark theme's background/surface/border tokens were also overridden in `index.css` (unlayered, so they win over HeroUI's own `@layer base` dark theme regardless of import order) to shift from HeroUI's near-neutral gray to a real blue hue (~258°) per an explicit "paleta mas azul de fondo" request.
  - **Deliberately not copied from the reference image**: the fictional "ark by codebius" product name (kept the real "Codebius Backup Manager" branding — renaming the product is a decision for the user to make explicitly, not to infer from a mockup); the notification bell with an unread-count badge (would imply a notifications feature that doesn't exist); and a user-account/avatar switcher in the sidebar footer (would imply multi-user support, which `project.md` explicitly lists as out of scope for v1) — the footer is just a static caption line instead.
  - **"Prefiero modals" got fully corrected, not just partially**: the first pass reasoned that only row-editing (which compresses a table's other columns) needed to move to a modal, and left "+ Nuevo cliente" as an inline panel above the table since it never had that specific problem. The user caught this immediately ("el 'crear cliente' no se abre en modal ahora. el editar si") — the actual ask was both, unconditionally. Fixed: create now opens the same `Modal` as edit everywhere (Clientes, and Conexiones from the start). Lesson recorded in memory: default every future create/edit screen to `Modal` from the outset, don't re-derive "is inline safe here" each time.
  - **Visually confirmed by the user via screenshot (2026-08-24)**: sidebar, stat cards, gradient pill buttons, and the disabled-not-dead styling on the unbuilt nav items all render as intended. Claude itself still has no browser tool in this environment — verified beforehand only via a clean production `pnpm --filter ui build` and the dev server responding.
- **Conexiones screen implemented (2026-08-24)**, chosen from an explicit menu of options (Conexiones/Tareas/Historial) via AskUserQuestion since "otras pantallas" didn't specify which — Conexiones was picked as the prerequisite: Tareas can't meaningfully create a new task from the UI without a transport/database connection to attach it to. Lists every transport and database connection across active clients in one table (Cliente, Nombre, Tipo badge — SFTP/SSH/postgres/mysql/mariadb, Host:Puerto, Acciones), with a "Mostrar inactivas" toggle, "+ Nueva conexión" (`Modal`, from the start — no inline-then-corrected detour this time, see the lesson above), per-row Editar/Probar conexión/Desactivar-Reactivar. The create/edit modal has a type selector (SFTP / SSH / Base de datos, create-only — type/engine are immutable after creation, same as `engine` on `direct_dump` database connections) that swaps the field set: SFTP shows remote path + file pattern, SSH shows remote command + output path template + a cleanup checkbox, database shows engine/database name/SSL mode. A passphrase/password field is always optional on both create and edit ("dejar en blanco para no cambiar" on edit) — never re-populated with the real secret, since the server never sends it back, only a `*SecretRef`.
  - **Backend additions this required** (mirroring the exact `ClientsRepo` pattern): `TransportsRepo`/`DatabaseConnectionsRepo` both gained `update()` (patch semantics; `type`/`engine` deliberately not editable — switching them would need an entirely different required-field set per the table's own CHECK constraint, so create a new connection instead), `deactivate()`, and `reactivate()` — added in the same increment this time (not as a same-day follow-up like Clientes needed) since the pattern was now established and cheap. New CLI commands `transport:update`/`transport:deactivate`/`transport:reactivate`/`database-connection:update`/`database-connection:deactivate`/`database-connection:reactivate`. `serve` gained `GET /connections` (transports + database connections + a `{id,name}` client list, each row's `clientName` attached server-side, `?includeInactive=true` supported), `POST /transports` and `POST /database-connections` (dispatch sftp/ssh by a `type` field in the body), `PATCH`/`.../deactivate`/`.../reactivate`/`.../test` for both — the test routes reuse the exact same `testTransportConnection`/`testDatabaseConnection` helpers the task-level test-connection endpoint already used. A plaintext `passphrase`/`password` in the request body is resolved to a secret ref via the same `resolvePassphraseSecretRef`/`resolveSecretRef` helpers the CLI's own create commands use — never stored as plaintext, never passed through to the repo layer. 91 vitest tests now pass (added `transportsRepo.spec.ts` and `databaseConnectionsRepo.spec.ts`, 7 tests each).
  - **Not visually verified yet** — same caveat as always; build is clean and endpoints were curl-verified end-to-end (create/test/update/deactivate/reactivate, active-filtering) before wiring into the UI, but the rendered form/modal itself hasn't been screenshot-confirmed.
- **Tareas screen implemented (2026-08-24)**, chosen next via AskUserQuestion (Tareas/Historial/Detalle de cliente) — now that Conexiones exists, creating a task from the UI has something to attach it to. Mid-turn the user asked whether Conexiones/Tareas/Historial should also be reachable from a per-client page — confirmed that's exactly what "Detalle de cliente" is for and it's a good candidate for right after this, but not built yet (see "Not yet built"). Lists every task across active clients (Cliente, Nombre, Estrategia badge, Origen — the transport or database connection name, Horario, Acciones), "Mostrar inactivas" toggle, "+ Nueva tarea" `Modal` with an Estrategia selector (SFTP existente / SSH remoto / Conexión directa a BD, create-only, same immutability rule as Conexiones' type selector) that filters the Transporte/Conexión-de-BD dropdown to that client's active connections of the right kind, and warns inline if the selected client has none yet (pointing at Conexiones rather than silently allowing an impossible task). Picking a `direct_dump` database connection auto-fills `dbEngine` from the connection's own `engine` rather than asking twice. The create modal also has an optional Horario (HH:MM + habilitado) so a task can be scheduled in the same step instead of a separate call — under the hood this still does `create` then `setSchedule` as two backend calls, matching the CLI's existing two-command split. Per-row actions reuse **existing** endpoints rather than adding new ones: "Ejecutar ahora"/"Probar conexión" call the same `/tasks/:id/run`/`/tasks/:id/test-connection` (and `runTaskNow`/`testTaskConnection` helpers) the Dashboard already uses.
  - **Backend additions**: `TasksRepo` gained `update()` (name/retention only — `strategy`/`transportId`/`databaseConnectionId`/`dbEngine` deliberately immutable, same reasoning as Conexiones' type/engine), `deactivate()`, `reactivate()`. New CLI commands `task:update`/`task:deactivate`/`task:reactivate`. `serve` gained `GET /tasks` (each row's `clientName`/`transportName`/`databaseConnectionName` resolved server-side, `?includeInactive=true` supported), `POST /tasks` (dispatches fetch_existing/remote_dump/direct_dump by a `strategy` field, optionally calls `setSchedule` right after creation if `scheduleTime` was given), `PATCH /tasks/:id`, `POST /tasks/:id/(deactivate|reactivate)`, and `POST /tasks/:id/schedule` (a thin wrapper around the existing `setSchedule()`, exposed separately from the name/retention `PATCH` since the CLI already treats scheduling as its own concern via `task:set-schedule`). 96 vitest tests now pass (`tasksRepo.spec.ts` gained 5 more).
  - **Not visually verified yet** — build is clean, all new endpoints (create with an inline schedule, update, schedule-only change, deactivate, reactivate) were curl-verified end-to-end before wiring into the UI.
- **Real branding wired in (2026-08-24)**: replaced the sidebar's placeholder gradient-square-plus-text brand mark with the actual full logo lockup (`AppShell.tsx` now renders `assets/arkode-logo-completo.png` directly, sized to the sidebar's width), set a real favicon (`public/favicon.png`, the isotype mark — the Vite-scaffold `favicon.svg`/`icons.svg` leftovers were unused and deleted), and the window title changed to "Arkode". The sidebar footer is now a real link to codebius.com reading "Arkode by Codebius", replacing the placeholder "Uso local en este equipo" caption from the redesign.
- **Layout/hierarchy polish (2026-08-24)**, from direct feedback on the rendered redesign: (1) the four screens' outer wrapper changed from `mx-auto max-w-6xl` to `max-w-[1600px]` (no `mx-auto`) — on a wide sidebar+content layout, centering a narrow column left a genuinely wasted ~290px gutter next to the 240px sidebar; content now hugs the sidebar with normal padding instead. (2) `StatCard` gained an `alert` prop — a stat representing overall system health (zero recent successes, any errors) now gets a filled icon background, a colored border, and a colored number instead of the same neutral tinted treatment every other stat gets, so a bad number reads as urgent rather than blending in. (3) Row-level secondary actions (Probar conexión, Ver error) were visually too close in weight to the primary action (Ejecutar ahora) — first tried muting them as smaller/quieter text pills, then per a follow-up suggestion converted to icon-only buttons with a native `title` tooltip (`IconButton.tsx`, reused by Dashboard and Tareas) using two new icons (`PulseIcon`, `EyeIcon` in `icons.tsx`) — deliberately a plain `<button title=...>`, not HeroUI's own `Tooltip` component, same complexity-avoidance precedent as `Table`/`Modal`/`Switch`. (4) Dashboard's "¡Hola, Codebius!" greeting was dropped for a plain "Dashboard" heading — this is a single-user local tool, a personalized daily greeting read as an unwanted SaaS-multiuser signal (same principle as declining the mockup's notification bell/account-switcher earlier).
  - **Flagged for later, not built**: a discrete system/scheduler status line above the task table (e.g. "● Arkode activo · Próxima ejecución 03:00") — the user described this as something they'd add "eventualmente," not now. Computing "próxima ejecución" needs the soonest `schedule_time` among enabled+active tasks that haven't run yet today, which doesn't exist as a query anywhere yet — treat as a real future increment, not a trivial add-on, when it's actually requested.
- **Historial screen implemented (2026-08-24)**, chosen via AskUserQuestion (Historial/Detalle de cliente/Logs) as the recommended pick since it's also the prerequisite for Detalle de cliente to have something to show. Read-only — no create/edit/deactivate, this screen's whole job is showing what already happened. Lists every backup run (any status, not just successes — this is the first place in the UI that surfaces `Pending`/`Running`/`Producing`/`Validating` rows too, not just terminal ones) across all clients, newest first, with Cliente/Tarea filter dropdowns (populated from `fetchTasks({ includeInactive: true })` so a run belonging to a since-deactivated task or client doesn't just disappear from history) and a "Ver error" icon-button that expands the full `error_message` inline, reusing the same `IconButton` pattern from Dashboard/Tareas. Two new format helpers in `lib/format.ts`: `formatDuration()` (seconds/minutes/hours) and `formatDateTime()` (compact absolute timestamp — age-only isn't enough once you're scanning 200 rows of history).
  - **Backend addition**: `RunsRepo` gained `listRecent(opts?: { taskId?; clientId?; limit? })` — the first genuinely new read query needed for this screen; every other list-scoped-to-a-task/client method already existed for other purposes (retention, dashboard) but none returned *every* run across the whole app. Capped at 200 by default so a long-lived install never loads its entire history in one request. New CLI command `run:list`. `serve` gained `GET /runs` (query params `taskId`/`clientId`/`limit`, `clientName`/`taskName` resolved server-side via direct `getById` lookups — deliberately not filtered to active clients/tasks, since a historical run shouldn't vanish just because its task was later deactivated). 101 vitest tests now pass (new `runsRepo.spec.ts`, 5 tests — the first dedicated test file for this repo).
  - **Not visually verified yet** — build is clean, `/runs` and its filters were curl-verified before wiring into the UI.
- **Detalle de cliente implemented (2026-08-24)**, the natural follow-on now that Historial exists too — this is the per-client drill-down screen `project.md`'s own screen list calls for, reached by clicking a client's name in Clientes (only when active; an inactive client's name renders as plain text, not a link — nothing to drill into once it's deactivated). Required **zero backend changes**: it's pure composition over endpoints that already existed (`fetchClients`, `fetchTasks`, `fetchConnections`, `fetchRuns`), each called with `includeInactive: true` and filtered client-side by `clientId` — a real payoff of every prior screen's data already being fetchable in bulk. Shows the client's header info (description, local base path, retention policy), its tasks (name, strategy, schedule, with the same Ejecutar ahora/Probar conexión actions as Tareas), its connections (name, type badge, host, Probar conexión), and its last 15 runs (read-only, same shape as Historial). `Clientes.tsx` gained an `onSelectClient` prop and `App.tsx` gained `selectedClientId` state — deliberately not a URL route (this app has no router, and adding one for a single drill-down wasn't worth it) — navigating away via the sidebar resets the selection back to the list.
- **Logs screen implemented (2026-08-24)**, requested directly ("logs, paginados y con filtros por fecha/termino de busqueda, accion?"). This is the first UI surface for `log_events` — that table already existed and its own schema comment said "condensed, queryable rows for the UI's Logs screen" (see the "Logging" section above), but nothing had ever queried it: `LogEventsRepo` was write-only (`append()` only) until now. Added `listRecent(opts)` (search/step/level/date-range filters, `limit`/`offset` pagination, returns `{ events, total }` so the UI can compute page count) and `listDistinctSteps()` (powers the "acción" filter dropdown from whatever step names have actually been logged — `connect`/`produce`/`download`/`validate`/`result`/`retention`/`recovery` today — rather than a hardcoded list that would drift from the orchestrator's real vocabulary). New CLI command `log:list`. `serve` gained `GET /logs` (`search`/`step`/`level`/`from`/`to`/`limit`/`offset` query params, plus `clientName`/`taskName` resolved per event via its `run_id` — cached per request since one run's several log lines all resolve to the same client/task, avoiding N+1 lookups for a busy run). 107 vitest tests now pass (new `logEventsRepo.spec.ts`, 6 tests).
  - **Not visually verified yet** — build is clean; `/logs`' search/step/level/pagination filters were curl-verified independently before wiring into the UI.
- **Configuración screen implemented (2026-08-24)** — the last v1 screen. `project.md` names this screen but gives zero detail on its contents, so scope was picked via AskUserQuestion: **diagnostics + config export/import**, not a settings *editor* (tool paths stay env-var-configured; making them UI-editable would mean moving them into `app_settings`, a real architecture change, not this increment). New read-only `getSystemInfo()` in `engine-core/src/status/getSystemInfo.ts` — resolves `appDataDir`/`dbFilePath`/`logsDir` from `paths.ts`, and for each of the five direct_dump tool-path env vars (`PG_DUMP_PATH`, `PG_RESTORE_PATH`, `PSQL_PATH`, `MYSQLDUMP_PATH`, `MYSQL_CLI_PATH`) reports whether it's set and whether the file it points at actually exists (`fs.existsSync`) — three distinct states shown as a badge: no configurada / configurada pero no encontrada / disponible. New CLI command `system:info`, new `serve` endpoint `GET /system`. 112 vitest tests now pass (new `getSystemInfo.spec.ts`, 5 tests).
  - **Config export/import got its first UI surface** — `exportConfig`/`importConfig` already existed and were fully tested via CLI, but had no HTTP exposure until now. `GET /config/export` sends the same `exportConfig('all', ctx)` payload the CLI produces, with a `Content-Disposition: attachment` header so a plain `<a href>` triggers a real browser download — no client-side JS needed for that half. `POST /config/import` takes the parsed JSON body directly (the browser reads the picked file via `File.text()` client-side and posts the parsed object, no multipart handling needed). The screen's import result table surfaces exactly what the CLI already reports: per-client created counts, `secretsNeedingReentry`, and per-client errors (e.g. re-importing an already-existing client name) without aborting the rest of the batch.
- **Three follow-ups on already-shipped screens (2026-08-24), all requested in one message**:
  1. **Backup file downloads are real now, no Tauri needed.** The user pointed out there was no way to get at the actual backup files — this was previously conflated with "abrir carpeta local" (which genuinely does need Tauri's `opener` plugin, since a browser can't launch a native file-explorer window). But *downloading one specific file the server already knows the path to* is a completely different, already-solvable problem: new `GET /runs/:id/download` streams `backup_runs.local_path` via `createReadStream(...).pipe(res)` with a `Content-Disposition: attachment` header — plain HTTP, works from any browser today. Deliberately checks `existsSync(run.localPath)` rather than trusting the DB column, because **retention deletes the file but never clears `local_path`** (see the "Retention" section) — a naive check would offer a dead link for any retention-pruned run. Wired into `Historial.tsx` and `ClienteDetalle.tsx`'s historial tab as a new `IconLinkButton` (`IconButton.tsx` gained this sibling — same look, but a real `<a href target="_blank">` instead of a click handler, since a multi-GB dump has no business going through a JS `fetch`+`Blob`). End-to-end verified by hand: pointed a real run's `local_path` at a temp file via direct SQL, downloaded it through the endpoint, confirmed byte-for-byte content match.
  2. **Detalle de cliente restructured into tabs** (Tareas / Conexiones / Historial, each showing a live count) instead of three always-visible stacked sections, per direct feedback ("en la ficha de cliente capaz haria pestañas").
  3. **Tareas' create flow became a single "+ Agregar backup" wizard** that can create the transport/database connection *and* the task together in one submit, instead of requiring a trip to Conexiones first — per direct feedback ("un solo form que... arme la tarea y la conexion en un solo paso"). The modal gained a "Conexión" toggle (Crear conexión nueva / Usar conexión existente, defaulting to nueva) — picking "nueva" reveals a purpose-built `NewConnectionFields` component (host/port/username plus SFTP/SSH/DB-specific fields, deliberately a smaller, simpler sibling of Conexiones.tsx's own form rather than a shared extraction, since it doesn't need that form's type/client selectors — the strategy and client are already chosen one step up in this same wizard). On submit: `POST /transports` or `POST /database-connections` first, then `POST /tasks` with the new id — two real backend calls behind one user action. **Known, accepted rough edge**: if connection creation succeeds but task creation then fails (e.g. a validation error), the new connection is left behind unattached — not an invalid state (a standalone connection is normal, manageable from Conexiones), and not worth building saga/rollback logic for an internal tool over. End-to-end verified via curl before wiring into the UI, then the test rows deactivated to keep the seeded demo data clean.
- **Two bugs fixed same day (2026-08-24), both reported directly by the user while testing the wizard above**: (1) "+ Agregar backup" only ever existed on the standalone Tareas screen — `ClienteDetalle.tsx`'s own Tareas tab was a read-only table with no create button at all, which is what the user actually hit ("no veo el boton 'agregar backup'" — they were on the client detail page, not Tareas). Fixed by extracting the whole wizard (`FormValues`, `EMPTY_FORM`, `defaultPortFor`, the create form fields, and `handleCreate`) out of `Tareas.tsx` into a standalone `TaskCreateWizard.tsx`, taking an optional `fixedClientId` that pre-fills and locks the client field — reused by both `Tareas.tsx` and `ClienteDetalle.tsx`'s Tareas tab now, instead of duplicating the form. (2) `Modal.tsx` had no internal scroll — a tall form (like this same wizard) could overflow the viewport with nothing to scroll it. Fixed: the header (`shrink-0`) stays pinned, the body scrolls (`overflow-y-auto`) up to `max-h-[85vh]`, and the outer overlay itself also scrolls as a fallback. Note the footer buttons (Cancelar/Crear) scroll with the body rather than staying pinned — a real, accepted simplification, not a fixed-footer layout, since splitting `Modal`'s API into header/body/footer slots would touch every existing caller for a tall-form edge case.
- **Non-daily task schedules added (2026-08-24)**, requested directly ("que el periodo de las tareas no sea unicamente diario? por ejemplo semanal, o determinado dia") — see the "Scheduler" section above for the backend side. `TaskCreateWizard.tsx`'s `ScheduleFields` (now exported and reused by `Tareas.tsx`'s edit modal too, replacing what had been two independently-maintained copies) grew a Diario/Semanal/Mensual segmented control: weekly reveals a row of day-of-week toggle pills (Lun..Dom, mapped to JS's own `0=Sunday..6=Saturday`), monthly reveals a 1-31 day-of-month number input with a note that a short month runs on its last day instead. **Two quick follow-up fixes the same day, both reported directly by the user testing this**: (1) the frequency selector was initially gated behind typing a schedule time first ("solo veo el campo horario") — confusing, since the gate wasn't obvious; now always visible. (2) "Horario" changed from a free-text "HH:MM" input to a native `<input type="time">` per direct suggestion — same string format the backend already expects, just with a real picker and no format-typo risk. `formatSchedule()` (new, in `lib/format.ts`) renders the combined state everywhere a task's schedule is shown ("03:00 · Diario", "03:00 · Lun, Mié, Vie", "03:00 · Día 15", each with "(deshabilitado)" appended when disabled) — replacing the plain `scheduleTime` display previously duplicated across `Tareas.tsx` and `ClienteDetalle.tsx`. End-to-end curl-verified (weekly, monthly-with-clamped-day-31, the weekly-with-no-days rejection, and restoring a task back to no schedule) against the real dev database before/after wiring into the UI.
- **Retention defaults added (2026-08-24)**, two related but distinct requests in the same conversation: (1) the task-creation wizard now prefills a new task's retention fields with the **selected client's own configured retention** (`retentionDefaultsFor()` in `TaskCreateWizard.tsx`) instead of leaving them blank with just a placeholder — still freely overridable per task, and re-applied whenever the client selector changes. Required widening `GET /connections`' `clients` array (and `ConnectionsData`'s type) to include `retentionCount`/`retentionDays`, which it didn't carry before. (2) The Clientes create form itself has no client to inherit from, so via AskUserQuestion the user picked a hardcoded suggested default of **10 backups / 30 días** for a brand-new client (`EMPTY_FORM` in `Clientes.tsx`) — still fully editable, and never applied to the edit form (`startEdit()` always overwrites it with the real stored client values first, so an existing client's actual `null`/configured retention is never silently overwritten by this default).
- **"Ver backups" per-client view added (2026-08-24)** — one of `project.md`'s five original per-client actions, chosen via AskUserQuestion as the next increment. Distinct from the existing Historial tab (which shows *every attempt*, any status, capped at 15 in this tab): this is real backups only — `RunsRepo.listBackups(opts)` (new in `runsRepo.ts`) filters to `status IN ('Success','Warning') AND local_path IS NOT NULL` (the same "no-op run isn't a real backup" pattern already used by retention's `listSuccessfulRuns` and the dashboard's `getLatestWithFileByTask`), paginated (`{ runs, total }`, default page size 50 at the repo/CLI level) rather than capped-and-truncated. New CLI command `backup:list --client/--task/--limit/--offset`, new `serve` endpoint `GET /backups` (same client/task-name enrichment pattern as `GET /runs`). `ClienteDetalle.tsx` gained a fourth tab, "Backups" (`BACKUPS_PAGE_SIZE = 20`, with Anterior/Siguiente pagination and an "X–Y de Z" count), listing task/date/size/status/download per row via the same `IconLinkButton` download pattern already used in Historial. 133 vitest tests now pass (`runsRepo.spec.ts` gained 5 more, covering the Success/Warning-with-file filter, Warning inclusion, ordering, per-task filtering, and pagination-with-total). Curl-verified against the real dev database (a client with one real backup, `total`/pagination fields correct) before wiring into the UI.
- **Config export UI gained a per-client selector (2026-08-24)**. The CLI's `config:export` already supported `--client <id>` (repeatable) alongside `--all`, but the UI's `GET /config/export` always exported every client — `Configuracion.tsx` only ever offered "Exportar todos los clientes". Fixed by having the endpoint read `clientId` query params (repeatable, defaulting to `'all'` when none given) and passing them through to the same `exportConfig()` the CLI already used; the UI gained a client `<select>` next to the export button (fetched via the same `fetchClients()` every other screen uses), and the button's label/downloaded filename adapt to the selection (a single-client export gets a name-based filename instead of the generic one). **Raised and deliberately declined in the same conversation**: given SSH private keys now export their real file content (see above), the user directly asked why DB passwords/SSH passphrases couldn't do the same. Answer, confirmed via AskUserQuestion: a private key file already sits as an unencrypted, freely-copyable file on disk — embedding its bytes in an export automates something already trivially possible and changes nothing about its real-world exposure. A password/passphrase is different in kind, not just degree: it lives *only* in this app's own encrypted secret storage — writing its actual value into a portable JSON file would undo that protection entirely, turning a leaked export (email, USB, an accidental git commit) into an immediately usable credential, which today it deliberately cannot be. The user chose to keep the existing re-entry-after-import flow rather than build an encrypted-export alternative (a real option floated but not built, since exporter/importer would each need to prompt for a separate export-passphrase to encrypt/decrypt the secret values — meaningfully more work for a want, not an ask).
- **Edit modals extracted and reused; client detail page can now edit, not just view (2026-08-24)**, prompted directly ("no se permiten editar tareas, conexiones, etc, desde dentro de la ficha de cliente?"). `Tareas.tsx`'s and `Conexiones.tsx`'s edit modals were each private to their own screen file — copy-pasting the same form logic into `ClienteDetalle.tsx` would have meant three copies to keep in sync. Extracted instead: `TaskEditModal.tsx` (new, built on `TaskCreateWizard.tsx`'s already-exported `Field`/`inputStyle`/`ScheduleFields`/`isScheduleValid`/`EMPTY_FORM`) and `ConnectionEditModal.tsx` (new, importing `ConnectionFields` and its helpers — `EMPTY_FORM`, `transportToFormValues`, `databaseToFormValues`, `toTransportInput`, `toDatabaseInput`, `isFormValid`, `ConnectionRow` — newly exported from `Conexiones.tsx`, which remains their one owner). Both `Tareas.tsx` and `Conexiones.tsx` now import their respective modal instead of keeping local copies; `ClienteDetalle.tsx`'s Tareas and Conexiones tabs gained matching "Editar" buttons wired to the same two modals — one edit form per domain, three call sites.
- **Icon+tooltip consistency pass, same conversation**: `Conexiones.tsx`'s "Editar"/"Probar conexión" were still full-text `Button`s, the one screen that had never gotten the `IconButton` treatment (`PulseIcon`/`EditIcon` — new — with a native tooltip) already applied everywhere else; now consistent. Every "Ejecutar ahora" button (`Dashboard.tsx`, `Tareas.tsx`, `ClienteDetalle.tsx`) gained a small `PlayIcon` next to its text — deliberately keeping the text, not replacing it: it's the one primary action per row, and the whole point of the earlier icon-only pass on secondary actions was to make *this* button stand out by contrast; going icon-only here would undo that hierarchy, and it triggers something with a real cost (network transfer, a DB dump) where a bare icon is worse for discoverability than a labelled one.
- **Client names are clickable everywhere they appear, linking to that client's ficha (2026-08-24)**, requested directly ("en todos los lugares donde aparece un cliente, deberia haber un vinculo a la ficha del cliente"). New shared `ClientLink.tsx` (extracted from `Clientes.tsx`'s own existing client-name-as-link, which now uses it too) wired into `Dashboard.tsx`, `Tareas.tsx`, `Conexiones.tsx`, and `Historial.tsx`. `Logs.tsx` needed one small backend addition first: `GET /logs` resolved `clientName` per event via its `run_id` already but never exposed the resolved client's `id` — added `clientId` alongside it (`LogEventRow` gained the field) so its client column could link too. Navigation itself is a single `goToClient(clientId)` added to `App.tsx` (sets `screen: 'clientes'` and the selected client id together) passed down as `onSelectClient` to every screen — clicking a client's name from anywhere always lands on Clientes with that client's ficha open, same as clicking it from the Clientes list itself.
- **Global pointer-cursor fix, same conversation**, caught by the user noticing client-name links didn't show a hand cursor on hover ("no deberian poner cursor 'manito' al hacer hover?"). Plain `<button>` elements don't get `cursor: pointer` by default in Chromium (the engine behind Tauri's WebView2 on Windows) — only `<a href>` does — and this app has many bare `<button>`s standing in for links/actions (`ClientLink`, `IconButton`, tab bars, segmented toggles). Fixed with one global rule in `index.css` (`button:not(:disabled) { cursor: pointer; }`) rather than adding the class to every button individually, since HeroUI's own `<Button>` already sets its own cursor and is unaffected.
- **`ClienteDetalle.tsx` bug fixed, same conversation**: its Tareas/Conexiones tabs always fetched with `includeInactive: true` (needed so the tab's own count badge and other tabs stay correct) but had no "Mostrar inactivas" toggle to hide them again — every deactivated task/connection was permanently mixed in with active ones, unlike the standalone Tareas/Conexiones screens which both already had this switch. Caught directly by the user ("no tenemos switch de 'mostrar inactivos' por lo que se estan viendo todas las conexiones y tareas"). Fixed by filtering client-side (`visibleTasks`/`visibleConnectionRows`, gated by a new local `showInactive` state) rather than re-fetching per toggle, since the bulk fetch was already happening anyway for the other tabs' sake — the toggle only shows for the Tareas/Conexiones tabs, next to the tab bar.

## Desktop shell (Tauri)

**Started 2026-08-24** — `packages/desktop-shell`, chosen as the next increment via AskUserQuestion. This dev machine had neither Rust/Cargo nor a C++ linker installed (a known blocker noted since the original architecture plan); both were installed this session (`rustup` via `win.rustup.rs`, Visual Studio 2022 Build Tools' `Microsoft.VisualStudio.Workload.VCTools` + Windows 11 SDK via `winget`) with the user's explicit go-ahead before running anything.

- **Wraps the existing `packages/ui`, doesn't scaffold a new frontend.** `pnpm create tauri-app` (the plan's original Phase 9 idea) assumes a fresh frontend and would have duplicated the already-built UI; instead, `packages/desktop-shell` is a bare package with `@tauri-apps/cli` as its only dependency, initialized via `tauri init --ci` with explicit flags pointing at the real paths: `frontendDist: ../../ui/dist`, `devUrl: http://localhost:5173`, `beforeDevCommand: pnpm --filter ui dev`, `beforeBuildCommand: pnpm --filter ui build`. The generated Rust (`main.rs`/`lib.rs`) is pure boilerplate — no business logic, no SQLite access — exactly the "thin shell" the architecture plan called for from the start.
- **Real branding wired in immediately**, matching the same "no placeholder identifiers" standard already applied everywhere else: `identifier` set to `com.codebius.arkode` (not the scaffold's default `com.tauri.dev`), window title/`productName` "arkode by codebius" (the user's own deliberate lowercase styling, also applied in `AppShell.tsx`'s sidebar footer/logo alt text the same day — not a casing inconsistency to "fix"), window sized 1400×900 (min 1024×700) instead of the 800×600 default, and the app icon set (`tauri icon`) from the real `arkode-isotipo.png` asset instead of Tauri's default rocket.
- **Verified end-to-end, not just "it compiles"**: `cargo build` succeeded (~3.5 min cold build), then `pnpm --filter desktop-shell dev` (`tauri dev`) actually launched a native `app.exe` window — confirmed both by process inspection (`MainWindowTitle` = "arkode by codebius") and by the user's own screenshot showing the real Dashboard, sidebar, and live data from the dev `.tmp-appdata` database rendering correctly inside the native window, not just the browser. This is the first real confirmation that the "thin shell" approach works end-to-end for this app, not just in theory.
- **Two plugins added the same day.** (1) `tauri-plugin-dialog`, for a real native folder picker — see "Carpeta local" in the UI section above for the full picture (Rust registration + capability permission + the `isTauri()`-gated UI button that degrades gracefully to the plain text input outside Tauri). Chosen first, via AskUserQuestion, specifically because it was the original reason Tauri was worth doing at all (a browser can never expose a real OS path). (2) `tauri-plugin-opener`, closing the last of `project.md`'s five per-client actions: "abrir carpeta local". `ClienteDetalle.tsx`'s header — the one place a client's `localBasePath` is already displayed — gained a `FolderIcon` `IconButton` next to it (new icon, `isTauri()`-gated same as the dialog picker), calling `openPath(client.localBasePath)` from `@tauri-apps/plugin-opener`; a failure (most likely the folder not existing yet, e.g. a client that's never had a successful backup) surfaces as an inline message rather than a silent no-op or an unhandled rejection. **Real gotcha hit and fixed**: `opener:default` alone was not enough — `local_base_path` is an arbitrary, user-configured path (could be any drive/folder), and Tauri v2's opener plugin scopes `open_path` by default; had to add an explicit `opener:allow-open-path` permission with `{ "path": "**" }` in `capabilities/default.json` to allow opening any path, not just a pre-known scope. This is a deliberate, low-risk broadening specific to this app: the path being opened is always one this same app's own config already trusted (a client's own configured backup folder), never untrusted/attacker-supplied input. Visually confirmed by the user: clicking the button opened the real Windows Explorer at that folder.
- **Third plugin, `tauri-plugin-single-instance`**, added same day: registered as the *first* plugin in the builder chain (it must intercept the app launch before anything else runs), with a callback that unminimizes/shows/focuses the existing `"main"` window rather than silently no-op'ing on a second launch attempt. Verified directly, not just by reading the docs: launched `target\debug\app.exe` a second time via `Start-Process` while the first instance was already running — the process list still showed only the original PID, confirming the second launch was redirected rather than opening a duplicate window.
- **`tauri-plugin-autostart` added (2026-08-24)**, chosen by the user after packaging itself was done, over `updater` (see "Packaging" for why `updater` specifically is deferred — needs its own signing key plus a hosted release manifest, neither of which exist yet). **Off by default, a real user-facing toggle, not silently enabled**: a "Iniciar Arkode automáticamente al iniciar Windows" `Switch` in Configuración (`isTauri()`-gated, same pattern as the folder picker/open-folder button), calling `@tauri-apps/plugin-autostart`'s `isEnabled()`/`enable()`/`disable()` — matches this app's consistent stance of never changing OS-level behavior (registry, startup, scheduled tasks) without the user explicitly asking. On Windows the plugin implementation writes to `HKCU\...\Run` via the `auto-launch` crate — standard, well-established mechanism, not something this session hand-rolled. `autostart:default` added to `capabilities/default.json`. **Fully verified for real, closing the loop on the input-injection gap below**: Claude couldn't click the toggle itself (same limitation as the `perMachine` UAC prompt), so the user tested it directly on the real `0.1.1` perMachine install — toggled it on, and Claude then confirmed via `HKCU:\...\Run` that a real `arkode` value was written pointing at `C:\Program Files\arkode\app.exe`, the actual installed exe path. No longer a trusted-by-convention claim; this one has a real registry key to point to.
- **Custom branded title bar, implemented and fixed for real the same day**, requested by the user after asking about the native Windows title bar ("no se puede ajustar color ni nada de eso asumo") — confirmed correct (no simple color property exists) and offered the real alternative: hide the native title bar entirely and draw one in the web content instead. `tauri.conf.json`'s window config gained `"decorations": false` (drops the OS-drawn title bar/border) and `"shadow": true` (keeps the drop shadow the native chrome would otherwise have provided). New `TitleBar.tsx`: a `data-tauri-drag-region`-tagged bar styled with the app's own dark theme (`var(--surface)`, matching the sidebar), the real `arkode-isotipo.png` + "arkode by codebius" on the left, and hand-drawn minimize/maximize-restore/close buttons on the right (four new plain-SVG icons in `icons.tsx`: `MinimizeIcon`/`MaximizeIcon`/`RestoreIcon`/`CloseIcon`, matching the existing no-icon-library pattern) calling `@tauri-apps/api/window`'s `getCurrentWindow().minimize()`/`.toggleMaximize()`/`.close()`. `isTauri()`-gated (renders nothing in the plain browser dev workflow, where there's no native window to control and `decorations` doesn't apply). `AppShell.tsx` restructured from a single flex row to a flex column (`TitleBar` on top, the existing sidebar+content row below in a `min-h-0 flex-1` wrapper so it fills the remaining height correctly) — `<main>` also gained `overflow-y-auto` so screen content scrolls within that remaining space instead of assuming the old single-row layout's implicit document-level scroll.
  - **A real bug, found only because the user actually clicked the buttons** — something Claude structurally cannot do in this environment (see the `perMachine` input-injection note above; the earlier "verified via screenshot" claim for this feature only ever covered visual rendering, never interactivity, a gap that turned out to matter). Reported directly: "no andaban los botones de la barra recien y no se podia mover." Root cause, found by inspecting the actual resolved permission set at `src-tauri/gen/schemas/acl-manifests.json` rather than guessing: `core:default` (the only core permission granted) expands to `core:window:default`, which — confirmed by reading its real contents — only includes **read/query** window permissions (`is-maximized`, `inner-size`, `theme`, etc., plus an `allow-internal-toggle-maximize` used internally by native decorations' own maximize button); it does **not** include the action commands `allow-close`, `allow-minimize`, `allow-toggle-maximize`, or `allow-start-dragging` that `TitleBar.tsx`'s buttons and drag region actually need. Those four were silently rejected by the permission system — no crash, just no effect, exactly matching what the user saw. **Fixed** by adding all four explicitly to `capabilities/default.json`. **Verified for real by the user** on the rebuilt dev version, not just re-asserted: buttons and window dragging both confirmed working ("perfecto, ahi anduvo todo").
- **A real, concrete gotcha from the `perMachine` verification came back to bite the very next build**, worth recording since it doubles as a live answer to "how does updating an already-installed copy work": the stuck, unkillable installer window left open earlier (see the `perMachine` bullet above) had `arkode_0.1.0_x64-setup.exe` locked, so the next `tauri build` failed with `failed to bundle project: Acceso denegado. (os error 5)` at the `makensis` step — Windows can't overwrite an `.exe` a running process still holds open. **Fixed by bumping the version** (`tauri.conf.json` and `Cargo.toml` both `0.1.0` → `0.1.1`) rather than fighting the lock — NSIS's output filename embeds the version (`arkode_<version>_x64-setup.exe`), so a version bump writes to a brand-new filename regardless of what's locking the old one. This is also, concretely, how re-installing an update over an existing install works today (there's no `updater` plugin yet — see above): bump the version, build a new installer, run it on the target machine. Tauri/NSIS handles the rest via the same "remembered previous install location" registry mechanism already described above — files get overwritten in place, the registry's version string updates, nothing needs to be uninstalled first. `allowDowngrades` defaults to `true`, so nothing today would even stop an older build from being installed over a newer one — worth setting to `false` explicitly if that ever matters. **Untested**: whether the installer requires the running app to be closed first (very likely, since Windows locks a running `.exe`, but not confirmed live this session).

## Packaging

**Started 2026-08-24** — chosen as the next increment via AskUserQuestion, with two real decisions confirmed by the user before any code: (1) **mysqldump/mariadb-dump (GPLv2) will not be vendored** — the installer only bundles Postgres's permissively-licensed `pg_dump`/`pg_restore`; MySQL/MariaDB client tools stay a separate manual install by whoever sets up the machine, exactly like today's dev-time env-var/tool-registry workflow, just not zero-touch for those two engines. (2) **installer format is Tauri's built-in NSIS bundler** (`tauri build`), not a hand-rolled WiX/MSI — the path already implied by the existing "Tauri as thin shell" architecture decision.

- **`engine-cli.exe` — compiles and runs standalone, verified for real (2026-08-24).** `@yao-pkg/pkg` (the maintained fork of the abandoned `vercel/pkg`, chosen since the original is unmaintained) added as a devDependency of `engine-cli`; `pnpm --filter engine-cli build:exe` (`pkg -t node22-win-x64 -o dist-exe/engine-cli.exe .` — must be pointed at `.`/the package.json, not the loose `dist/index.js` file directly, or pkg can't resolve `node_modules` and fails with `Cannot find module 'commander'`) produces a single ~89MB `.exe`. **The real risk going in was native addons** (`better-sqlite3`, `@primno/dpapi` — both ship prebuilt per-platform `.node` binaries) and pino's file-destination logging (which in some configurations uses worker threads with dynamically-resolved paths, a classic pkg-incompatibility trap) — **both worked with zero special handling**: pkg auto-detects and correctly bundles `.node` files without needing them listed in `pkg.assets`, and `pino.destination()` (as used here — not `pino.transport()`) writes synchronously with no worker thread involved. The **one thing that did need explicit config**: `engine-core`'s SQL migration files, read via `import.meta.dirname` at runtime (a dynamic path pkg's static analyzer can't trace) — fixed with one `pkg.assets` entry in `engine-cli/package.json` (`"../engine-core/dist/db/migrations/**/*"`).
- **Verified end-to-end against the actual failure mode that matters — running with zero trace of the monorepo or Node.js nearby**, not just "it launched": copied only `engine-cli.exe` into an empty temp folder (no `node_modules`, no `packages/`, nothing else) and ran the full real pipeline from there — `migrate` (SQLite via `better-sqlite3`), `client:create`, `transport:create-sftp --passphrase` (round-tripped through the real `@primno/dpapi` LocalMachine-scope encryption, not mocked), a real `task:run` against a fake key (failed for the *expected* reason — "Cannot parse privateKey," proving `ssh2`/`ssh2-sftp-client` executed correctly inside the snapshot — and correctly wrote a real pino log file to disk), and `serve --port` (HTTP server, `/status`/`/system` both responded correctly). This is the first real evidence that this app's original "must not require Node.js on the target machine" distribution constraint is actually satisfiable, not just theoretically planned for.
- **`dist-exe/` gitignored** (build output, same treatment as `dist/`).
- **`scheduler:install`'s Task Scheduler Action, fixed for the compiled exe (2026-08-24).** `process.pkg` is set to a truthy object only when code is actually running inside the `@yao-pkg/pkg` snapshot; `engine-cli/src/index.ts` now checks it (`Boolean((process as NodeJS.Process & { pkg?: unknown }).pkg)`) to decide the Task Scheduler Action's arguments. Under `process.pkg`, `process.execPath` is the real, callable exe path but `process.argv[1]` is a *fake* in-snapshot path (`C:\snapshot\...`) that isn't a real file on disk — passing it as an argument (the old, dev-only behavior) would have been a real, silent bug the first time this ever ran from a real install. Fixed: when `process.pkg` is set, the Action is just `<engine-cli.exe> run-due --task <id>` (no script-path argument at all); under plain `node dist/index.js` (today's dev workflow), behavior is byte-for-byte unchanged. Verified without touching a real Windows Scheduled Task: built a throwaway script calling `buildTaskDefinitionXml()` directly with both branches' real `command`/`arguments` values and confirmed the generated `<Command>`/`<Arguments>` XML elements for each — the pkg branch correctly omits the snapshot path, the dev branch matches the pre-existing, already-verified-live XML exactly.
- **`engine-cli.exe` wired as a Tauri sidecar (2026-08-24).** `tauri-plugin-shell` added to `desktop-shell`'s `Cargo.toml`; `tauri.conf.json` declares `bundle.externalBin: ["binaries/engine-cli"]` (Tauri's sidecar convention: the real file must exist at build time as `binaries/engine-cli-<rust-host-triple>.exe` — confirmed this machine's triple is `x86_64-pc-windows-msvc` via `rustc -vV`). New `packages/desktop-shell/scripts/prepare-sidecar.mjs` builds `engine-cli` and copies the pkg-compiled exe into that exact path; wired into `tauri.conf.json`'s `beforeBuildCommand` (via a new `prepare-sidecar` package script) so a fresh `tauri build` always bundles an up-to-date engine. **Deliberately spawned only in production** (`lib.rs`'s `.setup()`, gated on `!cfg!(debug_assertions)`) — the already-documented dev workflow starts `engine-cli serve` by hand against the same port 4287, so auto-spawning a second copy in `tauri dev` would just fight it for the port; this gate means the existing, already-verified dev workflow is completely unaffected by any of this. The spawned child's handle is stored in app-managed state and killed on `RunEvent::ExitRequested`, so closing the app doesn't leave an orphaned `engine-cli.exe serve` silently holding the port and the SQLite file — a real concern once there's no dev terminal to Ctrl+C.
- **`pg_dump.exe`/`pg_restore.exe`/`psql.exe` vendored (2026-08-24)** — the user confirmed (AskUserQuestion) mysqldump/mariadb-dump stay unvendored (GPLv2; separate manual per-machine install, unchanged from today) while Postgres's permissively-licensed client tools get bundled for real. New `packages/desktop-shell/scripts/prepare-pg-tools.mjs` downloads EDB's official Windows x64 "binaries" zip (`get.enterprisedb.com/postgresql/postgresql-<version>-windows-x64-binaries.zip` — confirmed this is EDB's real, current distribution URL via WebFetch against enterprisedb.com's own download page, then verified by hand that it 302-redirects to a real ~344MB `application/zip`), extracts only `bin/`, and **caches the extraction locally** (`.build-cache/`, gitignored) so re-running the script doesn't re-download 344MB every build — only the first run (or a version bump) touches the network. Only `pg_dump.exe`/`pg_restore.exe`/`psql.exe` and the `.dll`s they need get copied into `src-tauri/resources/pgsql/bin/` (declared as a Tauri `resources` entry); **confirmed by hand, not assumed, that EDB's `bin/` also ships ~13MB of wxWidgets DLLs for StackBuilder (a GUI installer tool never used here) plus a stray `testplug.dll`** — copied the 3 target exes alone with the rest of `bin/`'s DLLs into an isolated folder and confirmed `--version` works for all three without the wx*/testplug files, so the script explicitly excludes them (51MB vendored instead of 65MB). `lib.rs`'s production sidecar spawn resolves the installed resource path (`app.path().resolve("resources/pgsql/bin/<name>", BaseDirectory::Resource)`) and passes it as `PG_DUMP_PATH`/`PG_RESTORE_PATH`/`PSQL_PATH` env vars to the spawned `engine-cli.exe` — **zero engine-core/engine-cli code changes needed for this**, since those are exactly the env vars the dump client/validator/connection-tester already read as their defaults.
- **The full pipeline verified end-to-end via two real `tauri build` runs and real silent installs — not just "the build succeeded."** First `tauri build` (before pg-tools vendoring existed) confirmed Tauri auto-downloads its own WiX/NSIS toolchains on first use (no manual toolchain setup needed) and produced both an NSIS `.exe` and a WiX `.msi`; silently installing the NSIS output (`/S /D=<path>`, no elevation needed) into a scratch folder confirmed the 89MB sidecar really gets embedded and extracted at install time (a real concern going in — the installer's compressed size looked almost too small to contain it). Second `tauri build` (after pg-tools vendoring) grew both bundles by exactly the expected amount (NSIS 27MB→41MB, MSI 38MB→59MB) and a second silent install confirmed the vendored files land at the *exact* path (`<install_dir>/resources/pgsql/bin/*`) the Rust code's `resolve()` call computes. To avoid colliding with this machine's real, already-running dev-time `engine-cli serve --port 4287` (would have crashed the *test* instance harmlessly, but risked confusing output), the final env-var-propagation check ran the *installed* `engine-cli.exe` directly with the same env vars Rust would set and confirmed `system:info` reports `PG_DUMP_PATH`/`PG_RESTORE_PATH`/`PSQL_PATH` all `exists: true` from that real installed layout — proving the whole chain (install → resource resolution → env vars → engine-core's tool defaults) end-to-end without needing to launch a second live GUI window against the user's own real session. Both test installs were cleanly removed via their own generated `uninstall.exe` afterward.
- **The real installed window itself was launched and visually confirmed the same day** — the user was away from the machine and asked Claude to test it directly. Installed to a scratch folder for real (not silently this time), launched `app.exe`, and took a real screenshot (`System.Drawing`/`System.Windows.Forms` via PowerShell, then read back with the Read tool): the window opened with the correct title ("arkode by codebius"), full sidebar/branding, and a working Dashboard reading the machine's real `%PROGRAMDATA%` database (0 clients — this test install had no `CODEBIUS_APP_DATA_DIR` override, so it genuinely pointed at the same production database the dev environment normally uses, read-only for this check) — the first time the packaged, installed artifact (not `tauri dev`) was confirmed rendering correctly, not just "the process started." Also verified the exit-cleanup logic actually works: closing the window terminated **both** `app.exe` and the `engine-cli.exe` sidecar (checked via `Get-Process` immediately after) — confirming `RunEvent::ExitRequested`'s kill call actually reaches the child process, not just compiles. Necessarily involved briefly stopping this machine's real dev-time `engine-cli serve --port 4287` process so the installed app's own sidecar could bind the port without colliding — restarted afterward, confirmed responding again, restoring the dev environment exactly as found.
- **Confirmed this is a real Windows installer, not a portable app — the user asked directly, so this was checked for real rather than assumed from Tauri's defaults.** Installing without a custom `/D=` path (this machine's true per-user default, since a prior custom-directory install had left the NSIS installer's "remembered previous location" pointing there) confirmed all three things a real installer is expected to do: (1) a proper "Apps & Features" / Add-Remove-Programs entry under `HKCU:\...\Uninstall\arkode` with `DisplayName`/`Publisher`/`DisplayVersion`/`UninstallString` all correctly populated; (2) a real Start Menu shortcut (`arkode.lnk`, correctly targeting the installed `app.exe`); (3) a working uninstaller reachable both directly and via that registry `UninstallString`, confirmed by running it and then explicitly re-checking (not just assuming) that the registry entry, the Start Menu shortcut, *and* the install directory were all actually gone afterward. **Switched to `installMode: perMachine` the same day**, raised directly by Claude (not the user) as a real architectural inconsistency worth fixing before this runs on an actual client server: the default `currentUser` mode installs `engine-cli.exe` inside *a specific person's* profile, but the Scheduled Task that invokes it runs as SYSTEM — if that user's profile is ever deleted, the SYSTEM-run task silently starts pointing at a path that no longer exists, even though SYSTEM itself never depended on that user existing. `perMachine` (installs under `Program Files`, registry under `HKLM`) matches what the rest of the architecture already assumes (SYSTEM-run tasks, `%PROGRAMDATA%` app data) — confirmed via AskUserQuestion before changing. **Verification hit a real, honestly-reported limitation of this environment**: `perMachine` requires elevation, and a live Windows UAC consent prompt cannot be completed by Claude — this environment can capture screenshots and close ordinary windows (via `CloseMainWindow()`, a real window message) but cannot inject synthetic mouse/keyboard input into the interactive desktop, confirmed by trying and failing via both `SendKeys` and raw `mouse_event`/`SetCursorPos` P/Invoke calls, neither of which moved the wizard forward. Indirect but solid evidence the elevation itself *did* fire correctly: once launched, the installer process could not be closed or killed by Claude's own non-elevated session (`Stop-Process`/`taskkill` both failed with Access Denied) despite running in the *same* Windows session (confirmed via `SessionId` on both processes) — the only way that happens is if the installer is genuinely running at a higher integrity level, i.e. it really did elevate. **Closed out for real the same day, once the user was back at the machine and could click through what Claude structurally couldn't.** Installing `arkode_0.1.1_x64-setup.exe` (the bumped version — see the version-bump bullet below) interactively: no UAC consent dialog appeared at all (this machine's admin account apparently has elevation auto-approved without prompting — a real, machine-specific finding, not universal Windows behavior), the proposed install folder was `C:\Program Files\arkode` exactly as `perMachine` should default to, and Claude then confirmed via direct registry query that `HKLM:\...\Uninstall\arkode` exists with the correct `DisplayName`/`Publisher: codebius`/`DisplayVersion: 0.1.1`/`InstallLocation` **and** `HKCU:\...\Uninstall\arkode` does *not* exist (no leftover duplicate from the old per-user mode) — this is no longer trusted-by-convention, it's confirmed. The installer's Finish page briefly showed a blank white webview before the real Dashboard rendered — confirmed with the user to be a one-frame loading flash before the SPA finishes its initial paint, not a startup failure; unremarkable, not a real bug. **A stray installer window was left open on the user's real desktop** earlier in this same investigation, at the Welcome page (never advanced past it, nothing written to disk) since Claude couldn't close it — the user closed it by hand once back at the machine, unblocking a subsequent `tauri build` that had failed with `Acceso denegado (os error 5)` because that stuck process still held the old `0.1.0` installer file open (see the version-bump bullet below for the actual fix, which didn't require waiting for the window to close).
- **Two follow-up questions the same day.** (1) *"capaz que haga acceso directo"* (Desktop shortcut) — confirmed via `@tauri-apps/cli`'s own `config.schema.json` that `NsisConfig` has no built-in toggle for this, but does expose exactly the right escape hatch: `bundle.windows.nsis.installerHooks`, a `.nsh` file with macros hooked into specific points of the generated installer/uninstaller. New `src-tauri/installer-hooks.nsh` adds a `NSIS_HOOK_POSTINSTALL` macro (`CreateShortcut "$DESKTOP\arkode.lnk" "$INSTDIR\app.exe"`) and a matching `NSIS_HOOK_POSTUNINSTALL` macro to remove it. **Verified for real**: rebuilt, installed, and confirmed the Desktop shortcut is created at this machine's *actual* Desktop path (`[Environment]::GetFolderPath("Desktop")` — this machine's is folder-redirected to `D:\Pablo\Desktop`, not the classic `C:\Users\<user>\Desktop`; NSIS's built-in `$DESKTOP` variable correctly resolves via the same Shell API regardless, so the redirect needed no special handling), then uninstalled and confirmed the shortcut, the Start Menu shortcut, and the registry entry were all three gone again afterward. (2) *"¿permite elegir dónde instalar?"* — **first answered wrong, then corrected the same day.** Reading `NsisConfig`'s schema and finding no `allowToChangeInstallationDirectory`-style key led to telling the user no directory picker exists at all — every verification up to that point had used silent (`/S`) installs, which skip the *entire* interactive wizard, including this page. The user accepted that wrong answer and explicitly declined building a custom `template` for it (a real decision, made on bad information). Caught only when the installer was later launched **without** `/S` for the branding screenshot below: NSIS/MUI2's standard "Choose Install Location" page — with a real `Destination Folder` textbox (pre-filled from the previous install's remembered location) and a `Browse...` button — was there all along, unconditionally part of Tauri's default template, needing no config at all. **Corrected immediately**: this was never actually a missing feature, so the earlier "decline the custom template" decision stands anyway, but for the right reason now (there's nothing to build — it already works) rather than the wrong one (it's impossible without a custom template). Lesson worth remembering: a silent-only (`/S`) install is not equivalent to testing "the installer" — it deliberately bypasses the entire interactive page flow, so any question about wizard *pages* (as opposed to end-state file/registry layout) needs a real interactive launch, not just `/S` + directory inspection.
- **Installer branding added and verified the same day**, chosen by the user ("hace lo del branding") from the earlier punch-list. Built two BMPs (NSIS requires classic 24-bit `BMP3`, not PNG) via ImageMagick from the existing brand assets (`packages/ui/src/assets/arkode-logo-completo.png`/`arkode-isotipo.png`, both real `RGBA` PNGs, confirmed via `magick identify` before compositing): `installer-assets/header.bmp` (150×57, the full "arkode by codebius" lockup on white — matches the light installer chrome) and `installer-assets/sidebar.bmp` (164×314, the isotype mark centered on the app's own real dark-navy background). That background color was computed precisely, not eyeballed: the UI's `--background: oklch(16% 0.03 258)` token was converted to sRGB via the standard OKLab formulas (a small one-off script, not a library) → `#060d1a`, so the installer's sidebar genuinely matches the app's own dark theme rather than an approximated "similar-looking" blue. Wired into `bundle.windows.nsis.headerImage`/`sidebarImage`/`installerIcon`/`uninstallerIcon` (the latter two reusing the existing `icons/icon.ico`) plus a top-level `bundle.copyright: "© 2026 Codebius"`. **Verified visually, not just "files exist"**: launched the installer interactively (screenshotted via the same PowerShell/`System.Drawing` approach used for the app window earlier) and confirmed the Welcome page's sidebar image, the Directory page's header image, and the copyright string in the footer all render exactly as designed — this same screenshot is what caught the install-directory-picker mistake above.
- **Installer language, same day, prompted by the user noticing the screenshots were all in English.** Tauri's NSIS default is `languages: ["English"]` only. Added `"languages": ["SpanishInternational", "English"]` to `bundle.windows.nsis` — `SpanishInternational` (the Latin American variant, not Spain's `Spanish`) chosen to match Codebius's own locale, confirmed both language files exist in NSIS's own bundled set (`%LOCALAPPDATA%\tauri\NSIS\Contrib\Language files\`). `displayLanguageSelector` deliberately left at its default `false` — internal-use installer, no need to ask. **Verified for real**: rebuilt and launched interactively again; with no selector shown, the installer auto-picked Spanish purely from this machine's OS locale (window title "Instalación de arkode," Welcome page "Bienvenido al Asistente de Instalación de arkode," buttons "Siguiente"/"Cancelar") — confirming the language-matching behavior the schema describes actually works as documented, not just in theory.
- **Code-signing evaluated and deliberately declined for now (2026-08-24)**, not skipped by oversight. Asked the user directly whether this installer is installed by Codebius itself (on client machines/servers they manage) or handed to non-technical end users to self-install — confirmed it's the former. That materially changes the cost/benefit: the unsigned installer's Windows SmartScreen warning is a one-time "More info → Run anyway" click per machine for whoever on Codebius's own team runs it, not a real trust problem in front of a client. Paying for a certificate — either **Azure Trusted Signing** (~US$10/month, no hardware token needed, the modern/cheap option) or a traditional **EV/OV certificate** from a CA like DigiCert/SSL.com (~US$200-400+/year, and since a June 2023 CA/Browser Forum rule change, OV certs now also require HSM/token-backed key storage, not just a portable `.pfx`) — isn't justified for this usage pattern. A **self-signed certificate** was also considered and ruled out as not actually solving the problem: it doesn't remove the SmartScreen warning for an "unknown publisher" unless that specific cert is also manually added to each machine's trusted root store, which only helps if Codebius already manages those machines via domain/GPO. Revisit this decision specifically if the distribution model ever changes (e.g. a client starts self-installing it).
- **`updater` plugin implemented and verified end-to-end for real (2026-08-24).** This repo (`packages/desktop-shell` and everything else) was pushed to GitHub for the first time this same day — `https://github.com/pablopramparo/arkode` — specifically to host releases for this feature; it had no remote before. `tauri-plugin-updater` + `tauri-plugin-process` (Rust) and `@tauri-apps/plugin-updater` + `@tauri-apps/plugin-process` (JS) added; `bundle.createUpdaterArtifacts: true` so `tauri build` also produces a `.sig` file per installer. A minisign keypair was generated via `pnpm tauri signer generate --ci` (the `--ci` flag is required to avoid an interactive password prompt this environment can't answer) — the private `.pem` is gitignored and never committed, only its public counterpart (`.tauri-updater-key.pem.pub`, safe — it's the verification key, embedded in `tauri.conf.json`'s `plugins.updater.pubkey`). `capabilities/default.json` gained `updater:default` and `process:allow-restart`. UI: a "Buscar actualizaciones" section in Configuración (current version via `@tauri-apps/api/app`'s `getVersion()`, `check()` → shows the available version → `downloadAndInstall()` → `relaunch()`) — a manual, explicitly-confirmed flow, not silent/automatic, matching this app's consistent stance on OS-level actions.
  - **A real, non-obvious problem found and fixed the same session: the repo started private, which silently breaks the standard Tauri+GitHub-Releases updater pattern.** The convenient endpoint URL every Tauri tutorial uses (`.../releases/latest/download/latest.json`) returns a 404 for a private repo even with a valid Bearer token — confirmed by hand with a real fine-grained PAT (scoped to `Contents: Read-only` on just this repo), not assumed. The reason: GitHub's authenticated API can only fetch a release asset by its **numeric ID** (`api.github.com/.../releases/assets/{id}`, confirmed working with the same token), and that ID changes every release — but Tauri's updater does one fetch to one static pre-configured URL expecting the manifest JSON directly there, with no built-in "resolve latest release, then find this asset by name" step. So a token alone doesn't fix a private repo for this specific "always find the current latest" use case without also building a small proxy to do that two-hop resolution server-side. **Resolved by making the repo public instead** (confirmed via AskUserQuestion, after the user asked "y si permito que otros la usen que pasa?" separately and confirmed this is genuinely just for the updater plumbing, not a real open-sourcing decision — "no es un soft que vaya a comercializar, es posta de uso interno"; the repo being *visible* doesn't grant anyone usage rights without an actual `LICENSE` file, which wasn't added) — the same convenient URL then returned `200` with the exact right content, zero auth needed, confirmed by hand. The fine-grained PAT was revoked immediately after, since it stopped being needed at all.
  - **`gh` CLI installed this session** (via `winget install --id GitHub.cli`) specifically so Claude could create/manage GitHub Releases directly instead of walking the user through the web UI each time — authenticated via `gh auth login --web` (a device-code flow: Claude captured the one-time code from the CLI's own output and the user entered it at github.com/login/device in their own browser, since a real OAuth consent step can't be done any other way).
  - **Verified end-to-end for real, including the exact failure mode that matters (a stale version detecting and applying a real newer one), not just "the plugin compiles"**: published `v0.1.1` as the first real GitHub Release (installer `.exe` + its `.sig` + a hand-built `latest.json` manifest, all three uploaded via `gh release create`) and installed it for real (`perMachine`, confirmed `HKLM` entry at version `0.1.1`). Bumped to `0.1.2` (`tauri.conf.json` and `Cargo.toml` both), rebuilt with the signing env vars set (`TAURI_SIGNING_PRIVATE_KEY` — the raw key **content**, not `_PATH`, since the `_PATH` variant didn't survive being set in a parent PowerShell process and inherited through pnpm's nested child processes; `CI=true` was additionally required to skip yet another interactive password prompt during the sign step itself, mirroring the earlier `signer generate --ci` finding), and published `v0.1.2` the same way. **The user then ran the actual update from the real installed `0.1.1` app**: clicked "Buscar actualizaciones," got "Nueva versión disponible: 0.1.2," clicked "Descargar e instalar," then "Reiniciar ahora" — and confirmed the reopened app now shows "Versión actual: 0.1.2." This is the single most complete verification loop of this entire packaging effort: a real user click, on a real installed copy, detecting and correctly applying a real signed update built and published the same session.
  - **The manual release process, now proven, for the next version bump**: bump the version in both `tauri.conf.json` and `Cargo.toml` → `tauri build` with `CI=true` and `TAURI_SIGNING_PRIVATE_KEY` (file content, not path) set → hand-edit a `latest.json` with the new version/notes/pub_date and the freshly-generated `.sig` file's content → `gh release create v<version> <installer.exe> latest.json --repo pablopramparo/arkode`. No CI/reproducible-build pipeline automates this yet — every release so far has been built and published manually from this one dev machine.

## Error handling

- A failure for one client must never stop backups for other clients — tasks are independent.
- Never silently delete partial downloads: write to a temp extension (e.g. `.part`) and rename only after transfer *and* validation succeed.
- The app must be able to recover cleanly from an interrupted run.

## Logging

Every run logs: start, connection, remote generation (if applicable), download, validation, retention application, result, duration, and the full error message on failure. Logs must exist as files in addition to SQLite records, so diagnosis is possible even if the app/DB itself is having problems.

## Not yet built

**Packaging/distribution now has a real, verified, working end-to-end path — see "Packaging" above for the full story**: `engine-cli.exe` (via `@yao-pkg/pkg`), the Tauri sidecar wiring, vendored `pg_dump`/`pg_restore`/`psql`, the `scheduler:install` fix for the compiled exe, and two full `tauri build` runs each verified via a real silent install (plus a real installed-window launch, screenshot-confirmed, with exit-cleanup verified) are all done and confirmed working (2026-08-24) — not just "should work in theory." What's genuinely still missing: the `updater` Tauri plugin (`autostart` is done — see "Packaging"'s own bullet), no CI/reproducible-build pipeline (every build so far has been manual and local), and this has only ever been built/tested on this one dev machine, never on a genuinely clean Windows install. Installer branding (header/sidebar images matching the real brand, icons, copyright, a Desktop shortcut) is done, and — corrected after an initial wrong answer, see "Packaging"'s own note — the installer already lets the user pick an install directory via NSIS's standard interactive wizard page, no custom template ever needed. **Two decisions resolved, not open**: the GPL bundling question for `mysqldump`/`mariadb-dump` — confirmed by the user (2026-08-24) as "don't vendor them," those stay a manual per-machine install, same as today's dev workflow; and **code-signing, deliberately declined** (see "Packaging"'s own note) since this installer is run by Codebius itself on machines it manages, not self-installed by end users — revisit only if that distribution model changes.

Also still real product scope, not yet built: the Tauri desktop shell's `autostart`/`updater` plugins (see previous paragraph — same gap, just restated from the shell's own perspective; `tauri-plugin-dialog`/`tauri-plugin-opener`/`tauri-plugin-single-instance`/`tauri-plugin-shell` are all wired in — see "Desktop shell (Tauri)"). `direct_dump`'s tool-version *compatibility gate* and a multi-version `pg_dump`/`pg_restore` registry — **both fully implemented as of 2026-08-24**, along with equivalent registries for `mysqldump`/`mariadb-dump` and a UI to manage all three (see the "direct_dump tool version management" note above) — the only remaining related idea is DBeaver-style auto-downloading of these tools, explicitly discussed and declined for now. (A real live `scheduler:install` registration, with the credentials question this used to depend on — see "Scheduler" and "Secrets storage" — is done, as of 2026-08-24.)

**CI pipeline added 2026-08-25** (`.github/workflows/test.yml`, `release.yml`) — see "Packaging"'s CI note for the full story. `test.yml` (engine-core's vitest suite on every push/PR, ubuntu-latest) is green. `release.yml` (build+sign+publish on a `vX.Y.Z` tag push, windows-2022) got real fixes from three genuine bugs its first attempts surfaced — a stale `windows-latest` VS version, a same-millisecond retention-ordering tie (see the `runsRepo.ts` rowid-tiebreaker fix below), and `importConfig.ts` eagerly resolving the real machine keys directory — but is still blocked on one more, not yet root-caused: `prepare-pg-tools.mjs`'s EDB zip download is now confirmed genuinely complete and correct on the runner (HTTP 200, exact expected byte count, `content-type: application/zip` — the earlier "EDB might be blocking datacenter IPs" theory was wrong, disproven by the same hardening that was built to catch it), but the subsequent `tar -xf ... --strip-components=1` extraction still fails with `--strip-components=1: Not found in archive` on the GitHub-hosted runner specifically — never reproduced locally, because this dev machine's `.build-cache/` was already populated from earlier real sessions, so the download+extract path had literally never been exercised fresh on any machine before this CI run. Prime suspect, not yet confirmed: a different `tar`/bsdtar resolved via PATH on a clean Windows Server runner (GitHub Actions runs steps through PowerShell 7) than whatever this dev machine resolves. **Does not block real releases** — the proven manual local process (this dev machine's own populated cache, used for every real release v0.1.1 through v0.1.6) is completely unaffected; only the fully-automated CI path is blocked on this one step.

**Backlog, requested 2026-08-25, not started — noted for a future increment, not to be picked up unprompted:**
- **Export a single connection+task pair (not a whole client) so it can be attached to an existing client on import.** Today's config export/import (see "Configuration export/import") is client-scoped both ways: `config:export --client <id>` exports everything under that client, and import *always creates a new client* — there's no way to move just one transport/task pair into a client that already exists on the target machine. Would need a smaller export shape (a transport or database connection + its task, no client wrapper) and an import path that resolves against an existing `clientId` instead of creating one.
- **FTP support alongside SFTP.** Today's `sftp`/`ssh` transport types are both SSH-based (`ssh2`/`ssh2-sftp-client`). Plain FTP is a different protocol/library entirely (no SSH host-key model — FTP's own trust story, if any, is different) and would be a new transport type, not a variant of the existing one; same "declare the case now, stub the implementation" pattern already used for `direct_dump`'s `mariadb` engine would fit here.

## UX direction

Desktop-first only (no mobile/responsive requirement), dark mode, dense/information-first layout. The dashboard's single job is answering, in about five seconds: **do all clients have a recent, valid backup?** Problems must stand out far more visually than healthy backups.

Avoid: giant cards, decorative gradients, charts without real utility, unnecessary animation, excessive modal usage. This is an internal technical tool, not a marketing surface.

V1 screens: Dashboard, Clientes, Detalle de cliente, Conexiones, Tareas de backup, Historial, Logs, Configuración. Per-client actions: run backup now, test connection, view backups, open local folder, view last error.
